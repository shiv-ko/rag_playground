# 20260716_071633 Q17採用版test N=3生成・提出zip作成

## 目的

Q17 filescope採用版でtest 100問を3run生成・多数決し、SIGNATE提出物を作成する。

## 結果サマリ

- 初回生成は約2時間で中断。`make_predictions.py`だけ`Pipeline(cache_dir=...)`を渡さず、102,263チャンクを毎回再埋め込みしている二重管理ドリフトが原因と特定。
- `.cache/embeddings_ruri-base.pkl`を提出経路でも使い、`--no-cache`で無効化可能に修正（`2615251`）。step-reviewのCONFIRMED「モデル更新時の旧ベクトル強制再利用」はオプトアウト追加で解消。631 tests pass。
- 再実行で索引構築は約66秒に短縮。test 100問×3run生成完了。run1 Q63のAPI 529はMissing票になったが、監査上は3runともQ63 Missingで最終結果に影響なし。欠落IDは全run 0。
- CSVは100行、ID 0〜99連番、2列、ヘッダなし、最大回答114 bytesで形式正常。旧版からの変更は9問。
- `submission_20260716_q17-filescope3run.zip`作成。zip内は`predictions.csv`1件のみ。SHA-256: `ceedc8029381df500326f6a022056cf82c3b33615792ba83dcf5906c3909cef7`。

## 成果物

- `predictions.csv`
- `experiments/predictions_stability_1784153751.json`
- `submission_20260716_q17-filescope3run.zip`（git管理対象外）

## 残課題

- 当環境にSIGNATEへのログイン・ファイルアップロード機能がないため、ユーザーがzipを手動提出する。
- 提出後のLBを`plan_0703.md`に記録する。

