# 2026-07-04 短答値のみ＋spreadsheet_state強化（実験A/B/B2採用・B3規約リスクでrevert）

## 目的

`docs/superpowers/plans/2026-07-04-value-only-and-spreadsheet-state.md` の実行。
§2残り火（Q17/Q18のofficial不一致）と§3先頭（spreadsheet_state: フィルタ・Pivot）。

## 結果サマリ

- **local多数決 0.1333 → 0.2833 / official 0.1167 → 0.2333**（1日の推移。朝のPhase 0時点はofficial 0.1333）
- 採用3実験: A=値のみ短答（`bc22924`）・B=フィルタ/Pivot構造化接続（`621d063`）・B2=calc fall-through＋階層pivotガード（`e212126`）
- **B3は規約リスクで不採用・全revert**: Q6のofficial不一致（正解は「…で抽出されたデータに対する平均 / ALP」の記述文）
  を正解文の型を模したテンプレート＋LLM非経由の直接回答で解消しようとしたが、step-reviewが
  「Q6正解文からの逆算（同型Q11/Q21の正解にこの接尾句は無い）＝competition.md『個別設問の正解を
  生成ルールに反映』への抵触」をCONFIRMED指摘。コードはe212126のまま、run JSONは証跡保存
- 詳細は `plan_0703.md` §2.3（判定表・学び・残課題）

## やったこと（コミットSHA付き）

1. 計画策定 `179ad03` → スキャナ拡張 `ab7b427`（autoFilter条件・hidden行数・小型シートセル。
   実装はsubagent、レビュー(sonnet)問題なし）
2. 実験A `bc22924`（値のみ。official較正で+0.05確認）
3. 構造化接続 `8876efc`（レビューCONFIRMED1件=集計列欠損の捏造fill経路→修正・再レビュー解消）
4. 実験B `621d063`（+0.13、Q11=フィルタ条件3/3 Perfect。Q6/Q21未達の原因=calc分岐早期returnを特定）
5. 実験B2 `e212126`（fall-through化。初版でQ21がIncorrect 3/3化→compact階層pivotのabstainガードで解消）
6. B3実装→step-review規約指摘→revert（証跡: `experiments/expB3_*`・`expB3b_*`）
7. Codex向け実装指示書はforkが策定 `b2c0f0a`（Q0/Q7/Q26/Q25）

## 学び

1. **規約の実効的な境界を1回踏みかけて確定させた**: GT参照は評価側（judge較正・triage）まで。
   正解文の表現・型を生成側に写すのは違反（「未知の質問でも同じ導出か」だけでなく
   「この形はどの実測から来たか」を自問する）。step-reviewの規約観点が実際に機能した
2. **保守ガードの効果を2回実証**: 引用ゲート緩和(§2.2実験②)に続き、compact階層pivotの
   部分条件回答もIncorrect直行だった。「曖昧なら出さない」は-1回避として常に正しい
3. official judgeは表現に極端に敏感（完全一致Perfect、区切り違いでMissing）。
   Q6型の記述文GTは実質「表現一致の抽選」— 深追いより他の問の回収が効率的
4. 実験の系譜: 1実験1変更を守ったことで、B2初版のQ21 Incorrect化・B3の規約リスクを
   それぞれ単独でrevert/修正できた

## 残課題

1. **Codexブロック**: `2026-07-04-phase2-remainder-for-codex.md`（Q0/Q7/Q26/Q25、完了時に提出提案）
2. Q21型compact pivotの本回収（outlineLevel/pivotTable XMLの階層抽出 — 15分の実在確認から）
3. Q17/Q18のゲート通過中-1リスク、Q6のofficial表現問題（規約適合の範囲で）
4. worktree除去（未実施のまま）。AGENTS.mdに未コミットの手元変更あり（artifacts再生成節の削除
   ＋Skills行へのstep-review追加 — ユーザー側の編集とみられるため未コミット・要確認）
