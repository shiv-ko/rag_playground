# 2026-07-04 Phase 2残課題実装（Codex）

## 目的

`docs/plan/2026-07-04-phase2-remainder-for-codex.md` のQ0/Q7/Q26/Q25対応を、`step-review` 手順で小分けに実装する。

## 結果サマリ

- Q0: `マーカーされている` が `image_or_graph` に誤分類される問題を修正（`8b07c3a`）。capability gateは脱出、ただし文脈不足でQ0自体は未回収。
- Q7: CalcSpecに `group_by` + `select` を追加（`725acfd`）。Q7はlocal Perfect、official較正でもPerfect。
- Q26: `list` と `closest_to_mean_list` を追加（`37e62f1`）。Q26は `train_0077、train_0216、train_0242、train_0722` でofficial単発Perfect。
- Q25: office_style構造化抽出結果をLLMに委ねず直接返すよう修正（`ef22058`）。単発スモークで `1. データ理解・EDA` 回収。
- 全テスト: `.venv/bin/pytest tests/ -v` は最終 `328 passed`。

## やったこと

- `src/utils/question_classifier.py`: office_style語の範囲内にある `マーカー` だけでは `image_or_graph` を付けないようにした。
- `src/generator/spreadsheet_calc.py`: `group_by/select`、`list`、`closest_to_mean_list` を追加。xlsx/Pivot系はstateへフォールバックする保守ガードを維持。
- `src/orchestrator/pipeline.py`: `structured_office_style` の抽出済み文字列を直接回答化。
- `step-review`: 各タスクでサブエージェントレビューを実施。Task 3レビューで別作業由来のgenerated_eval重複指摘が出たが、今回差分外として未対応。

## 学び

- N=3を修正途中で何度も回すとコストが大きい。以後は対象問スモーク→official単発→採用候補が固まった段階で必要ならN=3。
- Q26は単純な値列挙ではなく「フィルタ後平均との差が最小の行ID列挙」だった。`closest_to_mean_list` として一般化できた。
- Q25はartifact/structured_contextまでは正しく、失敗点は生成LLMの拒否だった。構造化抽出済みの値は直接返す方が安定する。

## 残課題

- Q0はcapability誤ゲート修正済みだが、最終回答はまだMissing。office_marks/対応ファイル文脈の追加診断が必要。
- Q21 compact pivotは引き続きMissing維持。outlineLevel/pivotTable XMLの階層抽出が必要。
- Q17/Q18のIncorrectリスク監視は継続。
