# 2026-07-05 pivotブロック（実験F）レビュー・検証・採用（Claude）

## 目的

Codex実装のQ21型compact pivot回収ブロック（`b265b12`）のレビューと、
指示書 `2026-07-05-pivot-block-for-codex.md` Task 5のClaude側担当分
（valid N=3・official較正・採否判定）の実施。

## 結果サマリ

- **レビュー承認（CONFIRMED問題なし）**: step-reviewテンプレート5観点＋PoC§4罠10件を全て確認。
  321テストPASS・artifact 24行・プール計上no-harm（pivotのみでプールが立つ案件なし）を独立に再検証
- **実験F採用**: `exp_pivotagg_valid`×3で**Q21 Missing→Perfect 3/3**、
  official較正 **mean 0.3833（過去最高**、C 0.3667 / D較正 0.3333）。
  official IncorrectはQ17のみで不変。plan_0703 §2.3に実験F行を追記

## やったこと（SHA付き）

1. **レビュー**（`b265b12`対象）: 罠10件（x/生値のcount判定・`x="-2"`除外・subtotal省略→sum・
   rowFields空/pageFields/隠しアイテムの保守スキップ・`<m/>`除外・rowItems不使用・rel chain解決）
   全対応を確認。プラン外の`pipeline.py`プール計上1行は必要な変更（漏れるとpool=0棄却）と判定
2. **valid N=3**（`exp_pivotagg_valid` 1783181875/1783181996/1783182122）:
   - Q21: 3/3 Perfect「Attrition = No、Gender = Female、MaritalStatus = Single、EducationField = Human Resources」
   - local多数決 0.3500→0.3333。ただし悪化側flipのQ17/Q26は**6run全て回答バイト同一**
     （「前回未接触」「train_0077、…」）＝judgeゆらぎで、パイプライン出力は無変化
   - `retrieved_sources`突合: コンテキスト変化は**Q6/Q21の2問のみ**。
     Q6は質問に「PivotTable」＋英語列名を含むため新パスが発火（想定内の置き換え）、
     回答は同内容で表現のみ変化（`Gender=Male, disease=1, Age=68`→`Gender = Male、disease = 1、Age = 68`）、
     local Perfect 3/3維持。Q18「4」1回・Q28直答化1回はコンテキスト同一＝生成ゆらぎ
3. **official較正**（`judge_calibration_1783182281.json`、多数決ラベルと一致するrun 1783181996で実施）:
   mean **0.3833**・一致率73%。Q21/Q11 official Perfect。official Incorrect=Q17のみ（顔ぶれ不変）。
   Q6はofficial Missing（学び3の表現brittleness。D較正のIncorrect=-1より改善方向、変更起因の悪化ではない）
4. 記録: plan_0703 §2.3実験F行＋残課題更新（Q21クローズ、pivot多dataField案件のterm_registry未接続を残課題④に）、
   next-steps §3チェック

## レビューで記録したPLAUSIBLE（修正不要・監視対象）

- term_registry展開未接続: 多dataField案件（かえで8・ひがし丘7・青潮8）で日本語指標名の質問は不発火
  （保守側。officialにQ21型×多pivot案件があれば取りこぼす）
- `"Pivot" in question` が大小文字区別（小文字pivotは不発火、保守側）
- 「複数候補から指標名一致で1つ選ぶ」正照合パスのテストが無い（単一候補フォールバックと
  複数不一致abstainのみ）— Q6で実データ上は正照合の発火を確認済み

## 学び

- **決定的コンテキストは回答も決定的にする**: Q21の回答は3run完全一致。構造化パスの
  「既存回答への影響ゼロ」検証は`retrieved_sources`の群間突合が最速だった（Phase 0の診断情報が効いた）
- local多数決meanの±0.02程度の変動は、回答バイト同一checkで「judgeゆらぎ」と即断できる

## 残課題

- 提出物生成（test 100問→predictions.csv→zip）と提出判断: 未提出差分は実験D（test Q7型）＋実験F
