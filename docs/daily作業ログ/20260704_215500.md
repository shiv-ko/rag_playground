# 2026-07-04 Phase 2残課題の検証完遂と提出物生成（Claude）

## 目的

Codex実装（`8b07c3a`〜`446eb57`）のレビューで見つかった検証ギャップを埋め、
指示書 `2026-07-04-phase2-remainder-for-codex.md` Task 5-4（提出判断）まで完遂する。

## 結果サマリ

- **HEAD最終構成のN=3＋official較正で採用確定**（`eaf6f77`）: **official mean 0.3667**
  （B2b 0.2333 → calcgroupby較正 0.2667 → 0.3667）。Q25/Q26/Q27 official Perfect、
  Q23 Acceptable 3/3に安定化、official IncorrectはQ17のみ（Q6解消で2→1、増加なし）
- **提出物生成**: test 100問 → `predictions.csv`（回答20 / Missing 80、最長61文字）→
  `submission_20260704_phase2-remainder.zip`。**SIGNATEへの手動アップロード待ち**
- 証跡整理: ガード前退行run等6本をコミット（`a0a3fdc`）、中断空run 1本削除

## やったこと（SHA付き）

1. **Codex実装レビュー**: 4コミットとも実装品質は良好。ただし (a) ef22058（office_style直返し、
   Q0/Q23/Q25共通パス）後のvalid runが1本も無い、(b) Task 5-4未実施、(c) 証跡run未コミット、を検出
2. **証跡コミット**（`a0a3fdc`）: group_by導入時にQ21が「Manager」「5」でIncorrect化した
   ガード前N=3（Codexが検出し`_SPREADSHEET_STATE_HINTS`で解消した経緯の証跡）
3. **HEAD N=3**（`exp_phase2rem_head_valid`×3、`eaf6f77`）: local多数決 0.3333→0.3000。
   低下分を目視で切り分け → Q25はGT完全一致「1. データ理解・EDA」を3/3 Incorrect誤判定、
   Q27は同一回答'0.010301'にP/M/Aのjudgeゆらぎ。**パイプライン出力自体は改善**
4. **official較正**（`judge_calibration_1783169170.json`）: mean 0.3667で上記の切り分けを確定
5. **提出物**: `make_predictions.py`のPipeline構成がrun_pipelineと一致することを確認
   （cache_dir無しは20260704_005800ログに明記済みの意図的仕様。ドリフトなし）→ test 100問生成 → zip
6. 記録: `plan_0703.md` §2.3にC検証行と学び4を追記、next-steps §3にチェック追加

## 学び

- **local judgeはIncorrect誤判定もする**（GT完全一致でも検索文脈から「無関係」と推論して-1化）。
  localのIncorrect増を見たら raw_answer目視 → official較正で確定してから判断する（plan_0703 §2.3学び4）
- 全問共通パス（pipeline）に触る変更は、対象問スモークだけでなくvalid全体のN=3を最終構成で1回は回す
  （今回Q23の巻き添え退行が無いことはN=3で初めて確認できた。実際は逆に安定化していた）

## 前回提出（phase1-2fix, 回答19問）との差分の目視

回答20/Missing 80。今日の全採用実験（A/B/B2/C）が反映され、長文回答→値のみ短答への
変更が大半。新規回答: test Q7（office_style直返し）/ Q25（ハイライト合計 -11850576）/
Q37 / Q97。回答→Missing化: Q42/Q52/Q76（守り方向）。

**-1リスク監視（LB照合用）**: test Q7は「抽出条件と集計内容を答えて」型の質問に
office_style直返しがハイライト文字列そのもの（`見込金額（税込）、:、4,675,000 JPY`）で
回答している。質問型とのミスマッチの可能性あり — LB結果が想定（valid official 0.3667比例）から
下振れしたら最初に疑う箇所。

## LB結果（提出後に追記）

**0.03333 — 前回提出（phase1-2fix）と完全同値**。詳細な乖離分析は plan_0703 §1.1.1 の新行に記録。
要点: valid official 0.1667→0.3667の改善がLBに現れず。公開LBは約30問サブセット（1/30刻み）なので
過剰適合しない。回答20問のタグ別トリアージ（classify_questionのみ使用・GT不使用）を実施:

- test回答率はタグ別に text_only 11/50、spreadsheet_state 3/19、office_style 2/9、
  state+office 2/6、calc系 2/9。**validの回答率~43%に対しtestは20%** — カバレッジが半分
- **-1型ミスマッチの実例**: test Q7「黄色ハイライトされている数値に対応するデータの抽出条件と
  集計内容を答えて」（pptx）に、office_style直返しがハイライト文字列そのもの
  `見込金額（税込）、:、4,675,000 JPY` で回答。同型のtest Q77（xlsx）は構造化パスが
  正しく「region=southeast、smoker=no の条件で抽出したデータのageの合計値=4674」と回答
  → **「条件・集計内容を問う」質問では装飾文字列の直返しをしない**ゲートが一般則として切れる
- 列挙形式の揺れ: test Q60 `AI-05, AI-09, AI-08`（カンマ+空白・順序不定）。GT慣例は「、」連結
  （valid Q0/Q26で確認済み）。official judgeは区切りに敏感（plan_0703 §2.3学び3）

## 残課題（優先順は plan_0703 §2.3 と同じ）

1. Q17/Q18のゲート通過中-1リスク（Q17はofficial Incorrect残存: 「前回未接触」vs GT「未連絡」）
2. Q21型compact pivot（outlineLevel/pivotTable XMLの階層抽出）
3. Q0のoffice_style文脈不足（「M02資料」→ファイル対応付けの汎用化、next-steps §3）
4. Q6のofficial表現問題
5. **SIGNATE提出（手動）→ LB結果をplan_0703へ記録**（local/officialとの乖離監視）
