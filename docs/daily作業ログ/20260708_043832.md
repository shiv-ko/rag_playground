# 20260708_043832 リポジトリ・作業ログ棚卸し → 次アクション計画ドキュメント化

## 目的

ユーザー指示: リポジトリと作業ログを確認して次にやるべきことを整理し、
A（実データ検証）とB（クラウド実装）を別ドキュメントにまとめてpushする。
本セッションはクラウド環境（`data/raw`無し・`.venv`無し）。コード変更なし・計画ドキュメントのみ。

## 棚卸しで確認した現在地

- valid official mean 0.4667（7/6較正）、LB最高 0.13333（7/5・3run安定化提出）
- 2026-07-07セッションの3コミット（`1cec020`/`782aead`/`c011861`）が**実データ未検証のままmainに滞留**。
  特に`782aead`（検索クエリからの案件名トークン除去）は全設問の検索結果に影響するため、
  未検証のまま次の実装を重ねるのが現状最大のリスクと判断
- 残課題の大半は実データ必須（rankfix N=3実験、primary_alias再生成、かえで復号→Q38、Q15診断）。
  一方、白峰regex修正・復号配線・Q87は合成フィクスチャでTDD可能

## 成果物

- `docs/plan/2026-07-08-realdata-verification-block.md`（A）:
  ローカル環境での検証4タスク（①rankfix採否確定 ②primary_alias再生成＋Q15/Q16
  ③かえで復号→Q38/Q79 ④3run安定化提出）。比較元は`vdiff2_valid_*`（official 0.4667）
- `docs/plan/2026-07-08-cloud-implementable-tasks.md`（B）:
  実データ不要の実装3タスク（①白峰`extract_dates`のスペース許容regex修正
  ②復号ユーティリティの`build_contract_registry.py`配線 ③Q87型answerer）。
  実データ確認はすべてAに委譲する相互参照構成

## 学び

- 復号配線には「開始年月日8桁」の鶏卵問題がある（契約書自体が暗号化されており中身から
  開始日を取れない）。ファイル名の`pw-<alias><8桁>`型汎用パターン抽出か、別ソース日付候補の
  試行（`InvalidKeyError`棄却）のどちらかを実装前に決める必要がある — Bドキュメントに設計論点として明記

## 残課題

- A/B両ドキュメントのタスク実施（着手順: A Task 1のrankfix検証が最優先。
  クラウド環境で続けるならB Task 1から）
