# 20260712_214935 現HEAD（analysisブロック+pwfix）3-run提出物の生成とLB実測

## 目的

`docs/plan/2026-07-12-next-steps.md` §1「Phase 4構成の提出とLB確認」の続き。Phase 4 analysisブロック（`59a3fb1`〜`70fd3c2`）とパスワード保護xlsx復号配線（`6692190`/`ea70afe`）が両方入った現HEADで、`--data-dir "data/raw/share/共有ドライブ"`を明示した3-run安定化生成を行い、提出・LB確認まで完了させる。

## 結果サマリ

- `make_predictions.py --runs 3`で`predictions.csv`を再生成（回答28→34件、`4b74e04`）。安定化監査は`experiments/predictions_stability_1783858608.json`。
- `submission_20260712_analysis-pwfix3run.zip`（回答34/Missing66）を作成し、直前の`phase4-imagegraph`提出との差分7問を分析（`f2be95c`）。うち6問（Q4/Q5/Q32/Q35/Q61/Q73）はanalysisブロックの狙い通りの回収で、Q4は誤答`age`→正しい`bmi`に修正されている。パスワード復号配線が対象と見込んでいたQ79/Q92は該当answerer未実装のため差分なし（想定通り）。
- ユーザーがSIGNATEへ手動提出、LB**0.16667（=5/30）**を確認。2026-07-09の`kaedecontractfix3run`と同値で過去最高に並び、直前の`phase4-imagegraph`（3/30）からnet+2/30回復（`282821b`）。GT非公開のため個別問題の因果は確定できないが、analysisブロックの決定的回答6問の一部が公開サブセットで正答化したとみられる、という解釈をplan_0703.mdに記録した。

## やったこと

1. `.venv/bin/python scripts/make_predictions.py --runs 3 --data-dir "data/raw/share/共有ドライブ"`相当で3-run安定化生成 → `predictions.csv`更新・`4b74e04`。
2. `submission_20260712_analysis-pwfix3run.zip`作成、前回提出とのpredictions.csv diff（7問）を分析し`plan_0703.md` §1.1.1に記録 → `f2be95c`。
3. ユーザーがLBへ手動提出、結果0.16667をplan_0703.mdへ追記 → `282821b`。

## 学び

- 「公開LBは約30問サブセット・1/30刻み」という既存仮説（[[lb-granularity-and-valid-mismatch]]）に、5/30という値が今回も整合した。
- 前回提出（phase4-imagegraph）でLBが3/30に落ちた際に容疑とされたQ9/Q40/Q87/Q94の非決定的パスは、今回のanalysisブロック追加後もLBが回復方向に動いたため、深刻な劣化要因ではなかった可能性が高い（ただし個別確認はできていない、次回提出前の裏取り候補として`plan_0703.md`に記録済み）。

## 残課題

- Q79/Q92向けの新規集計answerer（test限定2問）は引き続き未着手・着手判断保留。
- Q17（言い換えIncorrect）・Q18（章番号系の不安定Incorrect）はいずれも2026-07-04から繰り返し観測されている既知の残存Incorrectパターンで、次アクションとして着手予定（`docs/daily作業ログ/20260711_014012.md`にQ18の先行調査あり）。
