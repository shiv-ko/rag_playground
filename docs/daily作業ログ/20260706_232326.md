# 20260706_232326 契約ルールブロック実装

## 目的

`docs/plan/2026-07-06-contract-rule-block.md` に基づき、契約書由来の `contracts.jsonl`、契約計算、APR判定、パイプライン接続を実装する。

## 結果サマリ

- `scripts/build_contract_registry.py` を追加し、契約書docxと最終報告書pptx/pdfから契約方式、金額、単価、工数、丸め規則、契約期間、実績値を抽出するようにした。
- `src/generator/approval_rule.py` と `src/generator/contract_calc.py` を追加し、請求額差分、逆算単価、APR-M3横断集計、一部横断契約集計を決定的に回答するようにした。
- `StructuredArtifactStore`、`question_classifier`、`Pipeline` に `contract_rule` ルートを接続した。
- `artifacts/contracts.jsonl` を実データから生成した。10件中9件ok、かえで契約書は暗号化/非zipで既存queue同様にfailed。
- `scripts/build_registries.py` も再実行し、`project_registry.json` の `primary_alias` を再生成した。

## 検証

- `.venv/bin/python scripts/build_contract_registry.py`
  - `contracts=10`, `ok=9`, `failed=1`
- `.venv/bin/pytest tests/ -v`
  - 444 passed
- 実データスモーク:
  - ひがし丘 ACTH=155h10m 差分: `398,750円減額`
  - APR-M3横断: `SHR、合計7,480,000円`
  - AOBM 逆算単価: `22,000円`

## step-review

`$step-review` 指定に従い、gpt-5.4のレビューエージェントを2回起動したが、どちらも完了前にshutdownとなりレビュー結果を取得できなかった。
代替ルールに従いセルフレビューを実施し、NFC/NFD差で主略称変換・train.csv探索が外れる確実な問題を修正した。

## 残課題

- かえで契約書は暗号化/非zipのため契約artifactがfailed。既存の暗号化ファイル対応ブロックで復号後に再生成が必要。
- Q46の座席表連携、Q67/Q87の提案/FR差分やサンプル数条件は基盤のみで、専用回答ロジックは未完。
