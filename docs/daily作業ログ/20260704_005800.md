# 2026-07-04 提出ファイル生成とPhase 0（Task 1-2）マージ

## 目的

`docs/plan/2026-07-04-next-steps.md` の §1（Phase 0マージ）と §6（提出）を実行する。

## 1. 提出ファイル生成（test 100問）

- **着手前に重大な問題を発見**: `scripts/make_predictions.py` がPhase 1/2以前のままで、
  `project_aliases` / `term_registry` / `artifacts_dir` をPipelineに渡していなかった。
  そのまま生成すると**エイリアス検出・構造化パスなし**（＝今回の全改善が無効）の提出になるところだった。
  `run_pipeline.py` と同一構成に修正してから生成（`1140bab`）。
  - **教訓**: 提出経路（make_predictions.py）と実験経路（run_pipeline.py）の二重管理はドリフトする。
    Pipeline構築部を共通化するリファクタを検討（次回どちらかを触るときに）。
- 生成結果（`77c60aa`）: 100問・ID 0〜99重複なし・**回答19問 / Missing 81問**・最長247文字（1000トークン制約OK）
- 提出物: リポジトリ直下 `submission_20260704_phase1-2fix.zip`（zip内は`predictions.csv`のみ、SIGNATE規定形式）
- 期待値: 前回提出（LB **-0.067**、ゲート通過Incorrectが原因で負）に対し、今回はローカルIncorrect 0〜1の
  守り構成なので正転見込み。LB結果が出たら`plan_0703.md`に記録し、ローカルjudgeとの乖離（judge較正の基準点）に使う。
- **LB結果の記録待ち**（提出はユーザーが手動で実施）

## 2. Phase 0 Task 1-2 のmainマージ

worktree `.claude/worktrees/phase0-measurement-infra`（`8ceb314`ベース＝Phase 1/2以前）の2コミットを
cherry-pickし、現行コードに合わせてコンフリクト解消。**257件全テストPASS**。

- **Task 1（`ac612e9`）パース結果のディスクキャッシュ**
  - `Pipeline(cache_dir=...)` / `run_pipeline.py --no-cache`。`data_dir`のファイル構成（相対パス・mtime・サイズ）の
    フィンガープリントが変わらない限り `.cache/parsed_<fp>.pkl` から復元。
  - 92,218チャンクの再パース（30秒超）が2回目以降スキップされる。
  - 注意: `make_predictions.py`にはcache_dirを渡していない（提出生成は常にフレッシュパース）。
- **Task 2（`9c86d8e`）run結果への診断情報**
  - `PipelineResult.raw_answer`（ゲート前のLLM回答）・`retrieved_sources`（`ファイル::location`のリスト）
  - summaryに `elapsed_seconds` / `generator_tokens` / `judge_tokens` / `models`
  - `src/utils/paths.py`（repo相対パス化。symlink非解決の理由コメントあり）
  - **マージ時の適応**: worktreeはPhase 1以前のため`generate()`のゲートが1つしかない前提だった。
    現行はゲートが5箇所（能力外/空contexts/わかりません/確信度/引用）あり、gitの自動マージは引用ゲートにしか
    `raw_text`を付けなかったので、`looks_like_missing`・`should_answer`のゲートにも追加し、
    引用ゲート経由のraw_text保持テストを1本追加した。
  - これで次のrunから「どのゲートで何が落ちたか」がJSONだけで読める（今回のNFDバグ級の調査が大幅短縮）。

## 状態

- worktreeの2コミットは両方mainに吸収済み。worktree自体は未コミット変更なしなので
  `git worktree remove .claude/worktrees/phase0-measurement-infra` で除去可能（未実施）。
- Phase 0の残り: Task 3（retrieval recall）〜Task 7（切り分け表）。`docs/superpowers/plans/2026-07-03-phase0-measurement-infra.md`参照。

## 次アクション

1. SIGNATEへ提出 → LB結果を`plan_0703.md`へ記録（ローカル0.13〜0.22との乖離確認）
2. Phase 0 Task 3〜7（`2026-07-04-next-steps.md` §1の続き）
3. フリップ層の安定化（同§2）
