# 2026-07-05 LB乖離対策①: office_style直返しの抽出意図ゲート（実験D）

## 目的

LB 0.03333（前回と同値・相殺疑い）の乖離分析で特定した-1候補
（test Q7: 「抽出条件と集計内容を答えて」型質問にoffice_style直返しがハイライト文字列で回答する
型ミスマッチ）を、一般則のゲートで塞ぐ。

## 結果サマリ

- **採用**（`869a1ff`、証跡 `bfdadc1`）: 「〜を（すべて/全て）抜き出す/抽出する」の動詞用法のときだけ
  装飾文字列を直返しし、説明要求型はLLM生成パスへ流す `is_style_extraction_request` を追加
- **valid 30問の回答はC構成と全問バイト同一**（無影響が設計意図。効くのはtest Q7型のみ）。
  local多数決 0.3000→0.3500 / official 0.3333 の差分は全てjudgeゆらぎ
- テスト330件全PASS（+3件: 意図判定ユニット＋説明要求型の統合＋漢字「全て」）

## やったこと（SHA付き）

1. TDD: RED（説明要求型で`generate`が呼ばれないことを確認）→ GREEN（正規表現
   `を(?:すべて|全て)?\s*(?:抜き出|抽出)` — 「抽出条件」の名詞複合語はマッチしない）
2. step-review（sonnet）: CONFIRMEDなし。PLAUSIBLEの漢字「全て」取りこぼしをTDDで追補
3. N=3（`exp_officegate_valid`×3）＋official較正（`judge_calibration_1783177178.json`）→ 採用（`bfdadc1`）

## 学び

- **official judge自体も同一回答に対して判定がゆらぐ**: Q6の回答文字列
  `Gender=Male, disease=1, Age=68` は較正4回中3回Incorrect・1回Perfect（C検証較正のみ）。
  §2.3学び3（Q6のofficial brittleness）の定量実証。official較正も単発の絶対値でなく
  「どの問がIncorrectか」の変化で見る
- 回答が決定的パス由来の変更は、**新旧runの回答バイト比較**が最も安い無影響証明
  （judge経由のmean比較はゆらぎに埋もれる）

## 残課題（次ブロック候補、優先順）

1. **カバレッジ攻め**: test回答率20%がスコア上限0.20の壁。text_only Missing 39問（§5チャンク改善）、
   spreadsheet_state Missing 16問（Q21型compact pivot: outlineLevel/pivotTable XML）
2. Q17型言い換え-1（「前回未接触」vs「未連絡」、official Incorrect残存中）
3. 提出タイミング: 今回の変更はtest Q7の1問のみに効く見込みなので、単独では提出せず
   次のカバレッジブロックとまとめて提出するのを推奨（運用ルール4「無駄撃ちしない」）
