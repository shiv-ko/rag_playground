# 20260716_044837 Q17拡張子なしファイル名スコープ修正の再開・完了

## 目的

7月15日に未コミットで残っていた、質問文中の拡張子なし実在ファイル名（Q17の「カラム説明」等）を検索スコープへ反映する変更を再開し、検証・レビュー・コミットまで完了する。

## 結果サマリ

- `find_question_stem_matches()`を追加し、3文字以上・単語境界ありの実在stemだけをNFC+casefold照合。未知データから動的にファイル名を得るため、Q17固有ハードコードではない。
- 通常候補外の名指しファイルも`ProjectScopedRetriever`が補完。案件検出時は当該案件＋社内文書だけに限定。
- 実データvalid検索はrecall **0.793→0.862（25/29）**。Q17は`カラム説明.md`がrank 1、single_text 4/4。
- 629 tests pass、`git diff --check`成功。コミット: `45a2048`。

## step-reviewと修正

- CONFIRMED 1: 「カラム説明から／より」を日本語境界として認識しない。境界文字と回帰テストを追加。
- CONFIRMED 2: 一致時にHybridへ全チャンク件数を要求し、全質問でも重複パスを走査する。検索候補を`top_k*4`に戻し、名指しsourceだけ後段補完。source pathはadd時にNFC重複排除して照合量をチャンク数からファイル数へ削減。

## 残課題

- valid N=3+official較正は未実施。社内文書・valid質問を外部の生成/judge APIへ送る可能性があり、明示承認なしでは実行不可と判定されたため停止した。
- ローカル検索評価JSON: `experiments/retrieval_valid_1784144801.json`（未コミット）。

