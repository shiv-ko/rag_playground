# 2026-07-04 Phase 0 計測基盤の完成（Task 3〜7）＋ LB結果記録

## 目的

`docs/plan/2026-07-04-next-steps.md` §1（Phase 0完成・マージ）の残タスク（Task 3〜7）を、
subagent-driven-development ＋ `/step-review`（コミット前のhaikuレビュー）で完遂する。
あわせて §6 のLB結果を記録する。

## 結果サマリ

- **Phase 0 完了条件を達成**: 「変更→30問評価→flip確認」= 2分3秒＋flip数秒。
  Missing+Incorrect 24問を機械分類: **検索失敗10 / 生成失敗12 / 較正失敗(過剰ゲート)1 / 計測不能1**（`docs/plan/missing_triage_20260704.md`）
- **retrieval recall（valid・top-5・計測可能29問）: 66%**。spreadsheet_state 0/3・code_static 0/2 が全滅
- **judge較正: official (gpt-5.2-2025-12-11) 0.1333 vs local 0.15、一致率90%** → LB乖離はjudge差では説明不可
- **LB 0.03333**（phase1-2fix提出）を`plan_0703.md` §1.1.1に記録。0.03333=1/30ちょうど → 公開LBサブセット採点の疑い

## やったこと（コミットSHA付き）

1. **Task 3** retrieval recall単体評価（`68fc097`）: `src/evaluator/retrieval_eval.py`＋`scripts/eval_retrieval.py`。
   実装者の初回実データ実行が**recall 0.0** → 調査で`document_registry.jsonl`（NFD）と実パス（NFC合成混在）の
   完全一致比較バグを特定。比較境界をNFC正規化に統一し19/29=66%に。バグ版の証跡: `experiments/retrieval_valid_1783123035.json`
2. **Task 4** flip分析（`31bff62`）: `src/evaluator/flip.py`＋`run_eval.py`書き換え。step-review指摘で非run JSONガード追加
3. **Task 5** OpenAI CRAGジャッジ（`3f58499`）: 本番プロンプトのバイト単位一致をプログラム検証。ゼロ除算ガード追加
4. **Task 6** triage（`5b4d3d7`）: step-review指摘で`load_ground_truth`を`src/evaluator/ground_truth.py`に共通化
   （前日のmake_predictionsドリフト事故と同型の予防）
5. **Task 7** E2E実行: `phase0_valid_1783125252.json`（mean 0.15, P5/A1/M23/I1）。
   flip確認: phase2final比 改善1/悪化0/不変29（診断情報追加のみなので想定通り）。
   較正: `judge_calibration_1783125320.json`。triage表生成 → `plan_0703.md` §2.1 追記
6. LB 0.03333の記録（`6cff78a`）

## 学び

- **NFC/NFD類のバグは「もっともらしい0%/100%」で現れる**。recall 0.0を「検索が弱い」と読まず実データで
  突合文字列をバイト比較したのが正解だった。以後、0%系の計測結果はまず計測自体を疑う
- step-reviewの費用対効果は良好: haiku 4回で CONFIRMED 5件（うち採用4・誤検知却下1）、
  各タスク数分・実装をブロックしない。誤検知の却下理由は台帳（`.superpowers/sdd/progress.md`）に記録した
- **judge較正の結論が早くも効いた**: 一致率90%でローカルjudgeは信用できる → LB乖離の調査は
  「公開LBの採点対象サブセット」と「test固有の失敗（特にspreadsheet_state 21問）」に絞れる
- 当初仮説「過剰ゲートが主因」は実測で否定（1問のみ）。主因は生成失敗12・検索失敗10の二本柱

## 残課題

1. **next-steps §2（フリップ層の安定化）が次アクション**: Q18の過剰ゲート（今回runで実証）、引用の安定化
2. 生成失敗12問の`raw_answer`個別読み（どのタイプの生成ミスかの内訳化）
3. eval_retrievalの2回目実行も76秒（BM25構築が支配的でパースキャッシュ対象外）— 気になったら`KeywordStore`のキャッシュ化を検討
4. judgeゆらぎ（同一回答N回判定）の定量化は未実施（必要になったら）
5. worktree `.claude/worktrees/phase0-measurement-infra` の除去（吸収済み・未実施のまま）
