# 2026-07-12 Phase 4 analysis系ブロック実装・暫定統合検証

## 目的

`analysis_metrics` / `notebook_output` / `code_static`のprimary 12問へ、正解ラベルを回答生成に使わない決定的な構造化経路を追加し、実データで統合診断する。

## 結果サマリ

- Task 1〜4をコミット: `59a3fb1`、`8f0a94a`、`670b061`、`b2215e6`。
- Task 5で判明したanalysisタグfallthrough、test Q73未実装、valid Q28のsymbolic閾値はgap fixで修正済みだが未コミット。
- 全テスト **565 PASS**。
- 直接構造化診断では対象12問中11問が`structured:analysis`、test Q56のみ安全なMissing。
- valid N=3・official較正は、共有ドライブ由来内容の外部送信に対する明示承認不足で未実施。採否は暫定。
- `predictions.csv`、提出zip、ユーザー側既存daily logには触れていない。

## やったこと

1. `analysis_records.jsonl`をJSON / Notebook / Python ASTから決定的に再生成し、NFC/NFD、画像バイナリ除外、failure継続を実装（`59a3fb1`）。
2. metrics JSON path、コード既定値、CAT条件、交互作用列を回答する経路を追加（`8f0a94a`）。
3. configと元CSVを解決し、Notebook source/outputの実処理に従う相関再計算を追加（`670b061`）。
4. DOCX詳細値とmetricsのDecimal差分、PPTXモデル順位表のAccuracy抽出を追加（`b2215e6`）。OfficeParserは既存の表境界で足りたため変更していない。
5. full test診断 `phase4_analysis_test_diag_1783842034.json` / `phase4_analysis_test_diag_1783842090.json` を、data-dir=`data/raw/share/共有ドライブ`、questions=`data/raw/share/質問回答/questions_test.csv`で実行。外部LLM Connection errorが83/100問で発生した。
6. full runでQ4/Q5/Q32/Q61がタグ付与後も生成へfallthroughする真因を、`_process_one`の構造化許可タグ一覧からanalysis系4タグが欠落していたことと特定。Q35は`office_style`併記により偶然発火していた。RED統合テスト後に修正した。
7. One-Hot Encodingの実効閾値とCSV全列を実装と同じdtype規則で走査するQ73経路、Q28の実効`categorical_unique_limit=50`解決を追加した。

## 対象問の直接診断

- valid: Q4=`hist_gradient_boosting`、Q22=`season`、Q24=`Attr7`、Q27=`0.010301`、Q28=`object/string/categorical`かつ`unique_count < 50`。
- test: Q4=`bmi`、Q5=`6`、Q32=`BOROUGH__x__BLOCK`等6列、Q35=`0.90527`、Q61=`n_estimators=300 / learning_rate=0.1 / random_state=42`、Q73=閾値100未満・`Gender`。
- test Q56: y軸の実表示目盛りは元CSV再計算で保証できないためMissing。

## 学び

- classifierでタグが付くことと、`_process_one`が構造化処理を呼ぶことは別の条件であり、full-path回帰テストが必要。
- Notebook内の複数相関セルを一括解釈すると、別セルの`.abs()`や`.head(N)`が質問対象へ混入する。質問対象セルを先に限定すべき。
- One-Hot対象dtypeは実装側の列選択と一致させる必要があり、boolは本実装では数値側に分類される。

## 残課題

1. gap fixをstep-review後にコミットする。
2. 外部送信先と共有ドライブ内容の送信について明示承認を得た環境で、valid questions=`data/raw/share/質問回答/questions_valid.csv`をN=3実行する。
3. official較正で新規Incorrect 0と対象問のPerfect相当を確認してから正式採否を決める。
4. full test診断を接続可能環境で再実行し、対象外質問の回帰を確認する。
