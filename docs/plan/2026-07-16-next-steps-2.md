# 次アクション計画（2026-07-16 夜、cold実測後の再計画）

[`2026-07-16-next-steps.md`](./2026-07-16-next-steps.md)（同日朝）の後継。項目1〜2（提出・Q6対策）と項目3の実測までが完了し、cold実行の3時間超過が判明したため、残項目の優先順位を確定する。

## 現在地

- valid official mean **0.8333**、LB **0.23333（7/30）** で過去最高。official Incorrectは0。
- Q17ファイルスコープ修正（`45a2048`）・Q6不要集計値除外（`6ca3746`）は採用確定・提出済み（`submission_20260716_q17-filescope3run.zip`）。
- **3時間制限の実測でcold不適合が判明**（ログ `20260716_194413.md`）:
  - cold: parse 14秒、**embedding index 3時間32分49秒**。generation開始前に制限超過。
  - warm: index 66秒、単発E2E 189秒、標準N=3 E2E 445秒。
  - 支配項はparseではなく102,263チャンクの埋め込み生成。

## 優先順位の判断

strategy.mdの原則（該当設問数×改善確率×得点差）で見ると、項目2以降はいずれも数問分の改善だが、cold超過は**未知データでキャッシュが効かない場合にパイプラインが完走できず全問スコア喪失になりうるハード制約違反**。よってcold高速化を最優先とし、他のすべてに先行させる。

## 1. cold埋め込みインデックス構築の高速化【最優先】

102,263チャンク ÷ 12,769秒 ≈ **8チャンク/秒**は、バッチエンコードされていない（1件ずつ`encode`）かバッチサイズが極小である可能性が高い。

- [x] 診断: 文書は`batch_size=64`の1回呼び出しで、Lockは呼び出し全体のみ保護。（2026-07-16, `1505a3d`）
- [x] 診断: 実機はMPS builtだがavailable=FalseでCPU実行。102,386チャンク中24,188件が重複。（2026-07-16, `1505a3d`）
- [x] ルール確認: 機械生成cacheは再利用可能と判断。新規検収データ対応のためcacheのみには依存しない。（2026-07-16）
- [x] 修正実装: 同一バッチ内をhashで重複除去し、元のベクトル順序を維持。（2026-07-16, `1505a3d`）
- [ ] cold再実測: `--no-cache`でcold index、続けてcold N=3のE2Eを計測する。
- [x] フォールバック設計: [`2026-07-16-cold-fallback-design.md`](./2026-07-16-cold-fallback-design.md)。実装はcold再実測後に判断。（2026-07-16）

**採用条件**: cold N=3が3時間以内、目標2時間以内。かつvalid検索recall 0.862と最新valid官能スコアに退行がないこと。

## 2. 再現性検証

本番judgeは1回きり・シード固定のため、提出前フリーズの前提条件。cold問題の解決後に行う。

監査CLIは実装済み（`94e92c7`）。既存valid N=3では最終回答90%、answered/Missing・answer path・検索結果100%一致。test新規2回とN=3実測は未実施。

- [ ] 同一入力・同一設定でtest単発を2回実行し、最終回答・answered/Missing・answer pathの一致率を計測する。
- [ ] Q8/Q9/Q94に加え、全100問のflip一覧を出す。検索結果が同一かも比較し、検索・生成・ゲートのどこで揺れたかを分離する。
- [ ] N=3多数決後の一致率も記録し、不一致問は保守的構成の候補にする。

## 3. local judge較正の改善（実験サイクルの意思決定品質）

agreement 67%は採否判断そのもののリスク。API不要のオフライン分析から着手できるため、項目1〜2のAPI実行待ち時間に並行してよい。

- [x] 21較正を質問×回答×GTでNFC重複除去し、評価専用回帰データ基盤を実装。（2026-07-16, `f298e42`）
- [x] dev古15/holdout直近5をmanifest固定し、official判定ゆらぎをフラグ化。dev40件/holdout42件。（2026-07-16, `f298e42`）
- [ ] GT完全一致をMissing/Incorrectにする誤判定（Q17「未連絡」、Q25「1. データ理解・EDA」）を中心にlocal judgeプロンプトを改善する。
- [ ] 採用条件: holdout agreement 85%以上に加え、official Incorrectの再現率、mean絶対誤差、混同行列が現行より悪化しない。改善後は前向きにのみ適用する。

## 4. confidence thresholdのタイプ別探索

能力追加は収穫逓減に入ったため、較正・安定化へ軸足を移す。

- [x] 閾値引き上げのみを反実仮想集計するCLIを実装。（2026-07-16, `00990f1`）
- [x] 引き下げ候補は採点せず追加judge対象として分離。（2026-07-16, `00990f1`）
- [x] `question_labels.csv`は参照せず`routing_tags`のみを使用。（2026-07-16, `00990f1`）
- [x] dev/holdout分離・最低件数・稀少タグの共通閾値維持を実装。（2026-07-16, `00990f1`）
- [x] 最高スコア/保守的構成を分離出力。予備実測は両者holdout 0.6333で現行と同率。（2026-07-16, `00990f1`）

## 保留継続

- Q79/Q92向け新規集計answerer（test限定2問）・座席表OCR（test Q13/Q46）・test Q66画像特定: 期待値低、安全なMissing維持。
