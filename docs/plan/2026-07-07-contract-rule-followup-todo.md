# 2026-07-07 contract_rule ブロック残課題TODO

> 位置づけ: `2026-07-06-contract-rule-block.md`の実装（`afe0d2d`）とそのバグ修正
> （`01349af`/`88fe1d1`/`8ce3715`、詳細は`docs/daily作業ログ/20260707_000700.md`）・
> 評価run（`docs/daily作業ログ/20260707_002000.md`）を踏まえた次アクション一覧。
> 着手時は各項目ごとに実装計画を立ててからTDD（`dev-process`スキル）で進める。

## 現在地（2026-07-07時点）

test 100問診断run（`contract_fix_test_diag_1783351147.json`）で確認済み:

| Q | 状態 |
|---|---|
| 6, 23, 26, 31, 37, 55, 76 | 実装済み・発火確認済み（Q26/Q55は手計算と一致） |
| 38 | 閾値バグ修正によりMissingへ安全に後退（旧実装は白峰を誤ってAPR-M3と誤答） |
| 46, 67, 78, 79, 86, 87, 98 | 未実装（計画通り優先度低・別ブロック扱い） |

## 1. 【最優先】かえで総合病院契約書の復号 → Q38を真に解く

**理由**: Q38（APR-M3該当案件の集計）は現在Missingで安全側に倒れているが、正解は恒一会かえで総合病院
（医療案件＋契約金額が5,000,000〜8,000,000円未満の帯なら医療補正で本部長承認=APR-M3に該当する、
決裁基準§3.1の例そのもの）を含む可能性が高い。契約書`契約書_pw-kaede20250902.docx`が
`CDFV2 Encrypted`（パスワード保護）で`build_contract_registry.py`の抽出が`status=failed`のまま。

`docs/dataset_overview.md`に規則が明記されている:
> `データアステル社内規定_パスワード導出規則.docx`: 保護ファイルのパスワードを
> `DA-[案件略号]-[契約開始日8桁]-[拡張子コード]` の形式で導出する規則

- [x] `データアステル社内規定_パスワード導出規則.docx`を実際に読み、規則を確認済み（レビューで裏取り）:
      `DA-[案件略号]-[開始年月日8桁]-[拡張子コード]`（規則文書本体では「契約開始日8桁」ではなく
      「開始年月日8桁」表記）。案件略号は`社内用語集.docx`の主略称＝`project_registry.json`の
      `primary_alias`と一致（KAEDE等）。例: `DA-AOMINE-20250806-xlsx`
- [x] （部分完了・2026-07-07 `c011861`）`msoffcrypto-tool`によるOffice復号ユーティリティ
      `src/utils/office_crypto.py`を追加: `derive_office_password()`（`DA-[案件略号]-[開始年月日8桁]-
      [拡張子コード]`規則の純粋関数）と`decrypt_office_file()`（元ファイル非破壊・誤パスワード時は
      `InvalidKeyError`を伝播）。TDD9ケース（合成暗号化フィクスチャ、msoffcrypto-tool 6.0.0自体の
      暗号化側4KB未満バグを回避するパディング済み）で全件PASS。
      **未実施**: `build_contract_registry.py`への配線（呼び出し側の実装）は本セッションでは
      `data/raw`（実際の暗号化契約書ファイル）が無いため次アクションとして残す
- [ ] 復号成功後、`build_contract_registry.py`でかえでの契約行を`status=ok`にし、`contracts.jsonl`を再生成
- [ ] Q38の実際の回答が変わるか（かえでがAPR-M3に加わるか）を診断runで確認
- [ ] 同じ規則で`02.計画/スケジュール.xlsx`（かえで、`encrypted_file_queue.md`に記載の2件目）も復号できるか
      確認（Q79のDA担当者集計にも影響しうる）

**期待効果**: Q38が真に解ける可能性（+1問相当）。復号ユーティリティは他の暗号化ファイルにも汎用的に使える。

## 2. Q6の`final_amount_incl_tax`の妥当性確認

ひがし丘の最終報告書PDFが「実績工数の最終確定値は提示資料に含まれないため、請求金額欄は契約上の
見込工数170時間を用いた精算想定値として記載する」と明記しており、これは**仮定値であって真の実績確定額
ではない**。現状の抽出はこれをそのまま実績値として扱い、Q6（見込税込と最終請求額の差額）に対して
「0円減額」を確信度0.9で返す。

- [ ] validやtestの実際の採点結果（official judge較正）が出た際、Q6がPerfect/Incorrectのどちらになるか確認する
- [ ] Incorrectだった場合、`extract_report_values`に「見込工数を精算想定値として記載」等の仮定文言を検出する
      ロジックを追加し、該当時は`final_amount_incl_tax`をnull扱い（→Missing）にするフォールバックを検討する
- [ ] 逆にPerfectだった場合は現状維持でよい（無理に複雑化しない）

**注意**: 現時点では正解不明のため、着手前に採点結果を待つのが安全（勘で直すと逆に壊すリスクがある）。

## 3. 白峰信用リスク評価の契約期間抽出漏れ

`extract_dates`（`build_contract_registry.py`）の正規表現が「日?から」「日?まで」の直前にスペースを
許容しないため、白峰の契約書（「2025-05-13 から 2025-07-22 まで」とスペース入り表記）で
`start_date`/`end_date`が両方nullになっている。現状Q26の回答には影響しない（白峰はどのみち指定期間と
重複しない）が、他の横断集計（`contract_period_days`を使う設問全般）に波及しうる。

- [ ] `extract_dates`の正規表現に`\s*`を許容する（`日?\s*から`, `日?\s*まで`）
- [ ] 修正後`contracts.jsonl`を再生成し、白峰の`start_date`/`end_date`/`contract_period_days`が
      正しく埋まることを確認する回帰テストを追加

**優先度は低**（現状の回答に影響しないため、他タスクの合間で対応可）。

## 4. 未実装設問（cross_project側、優先度中）

`2026-07-06-contract-rule-block.md`のTask 6で計画済みだったがQ46のみ未実装。

- [ ] **Q46**（着手金が最も高い案件のESの内線番号）: `advance_payment_amount`の抽出は`8ce3715`で
      修正済みで値は入るようになった。座席表registry（ES=データアステル側担当者の内線番号）との
      結合ロジックが未着手。座席表(`座席表.pptx`)からの内線番号抽出資産の有無を確認してから着手
- [ ] **Q67**（APR-M2該当・完了案件・提案時金額とFR時金額が異なる）: `determine_apr_level`は使えるが、
      「提案書記載金額」と「FR記載金額」を突合するロジックが未着手。まず`00.提案`と`06.報告書`から
      金額を抽出できるか実データで確認してから設計する
- [ ] **Q87**（APR-M1該当・完了案件・サンプル数10000行以上）: `determine_apr_level`＋train.csv行数
      （既存の`_load_train_csv`相当ロジック）の組み合わせで比較的低コストに実装可能。Q31の
      `_answer_fixed_per_row`と同じtrain.csv行数取得パターンを流用できる

## 5. 未実装設問（テキスト抽出寄り、優先度低）

計画時点で「既存の検索+生成パスの引用精度向上で対応する方が自然」と位置づけ済み。新規registry投資は
不要だが、実際に検索パスが正しく引用できているかは未確認。

- [ ] Q78（ひがし丘のACTH200時間超の精算規定内容）: 契約書内の該当条文がretrieverで拾えているか確認
- [ ] Q98（TM案件のRATE変更日）: レビューで裏取り済み — 9件（かえで以外の全件）の契約書docx全文を
      RATE変更/単価変更/レート変更/単価改定で検索したが1件もヒットなし。`raw_billing_clause`の
      1200文字切り詰めが原因ではない（9件とも288〜620文字で切り詰め自体発生していない）。
      未確認として残るのは暗号化中のかえで契約書のみ（§1のパスワード復号が先行条件）
- [ ] Q52（みなみ野の「別契約」明記箇所抽出）: office_style/list_extraction系の既存パスで対応可能か確認

## 6. Q79・Q86（別ブロック、本ブロックの対象外）

- Q79（かえでのDA担当者1タスクあたり想定工数最大）は`schedule_tasks`registry（既存資産）の集計問題。
  ただし、かえで契約書と同様に**計画フォルダのファイルも鍵がかかっている可能性**（設問文に「ファイルに
  鍵がかか(っている)」という記載あり）→ 本TODOの§1（パスワード復号）が先行条件になりうる
- Q86（各案件のPP/契約書/PLAN/FRでDA側実施体制の人数）はpeople_registry拡張が必要で、契約金額計算とは
  性質が異なる。別ブロックとして切り出す

## 推奨順序

| 順 | 項目 | 期待効果 | 備考 |
|---|---|---|---|
| 1 | §1 かえで復号 | Q38（＋Q79の前提条件解消） | パスワード導出規則の解読が鍵 |
| 2 | §4 Q87 | 低コストで実装可能 | Q31と同じtrain.csvパターン流用 |
| 3 | §3 白峰日付バグ | 低リスク・低コスト | ついでに直す |
| 4 | §4 Q46/Q67 | 中コスト・要実データ調査 | 座席表/提案書FR突合の設計から |
| 5 | §5 Q78/Q98/Q52 | 既存パスの引用精度確認 | 新規実装は最小限 |
| 6 | §2 Q6 | 採点結果待ち | 勘で触らない |
| 7 | Q79/Q86 | 別ブロック | §1完了後に着手判断 |
