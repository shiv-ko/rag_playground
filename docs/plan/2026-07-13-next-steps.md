# 次アクション計画（2026-07-13策定）

> 位置づけ: `docs/plan/2026-07-12-next-steps.md`の続き。§1〜3はすべて達成済みのため本書は新規に作成する。
> 判断原則は `strategy.md` の「守りを先行」「該当問数 × 改善確率 × 得点差」「1実験1変更」に従う。

## 0. 現在地

- Phase 4（analysis / image_graph / パスワード復号）は採用済み・提出済み（LB **0.16667**、過去最高）。
- `f59c732`（Q18系文書種別略号のnamed-file boosting）はvalid N=3+official較正で新規Incorrect 0を確認し採用確定（`docs/daily作業ログ/20260713_213900.md`）。
- test predictions.csvを3-run安定化で再生成し`submission_20260713_docscope3run.zip`を作成済み（`009800c`）。**未提出**。
- 既知の残存Incorrectパターンは**Q17（言い換え）のみ**に絞り込まれた（Q18系は今回の変更で対策済み）。

## 1. 【最優先】docscope版の提出とLB確認

- [ ] `submission_20260713_docscope3run.zip`をSIGNATEへ提出する（ユーザーが手動実施）。
- [ ] LBを`plan_0703.md` §1.1.1に記録し、前回`0.16667`との差分を解釈する。

**完了条件**: 提出・LB・差分解釈が作業ログに残っている。

## 2. Q17（言い換えによるIncorrect）対策

**対象**: valid Q17（`pdays`列の値-1の意味、「未連絡」を「前回未接触」等に言い換えてIncorrect化）。2026-07-04から一貫して観測される唯一の未解決の既知パターン。

- [ ] 過去の較正結果（`judge_calibration_*.json`）を横断し、Q17がIncorrectと判定された全ケースのraw_answerを集めて言い換えパターンを類型化する。
- [ ] `temperature=0`によるブレ抑制は不可（claude-sonnet-5の制約、確認済み）。プロンプト側で「模範解答の表現に寄せる／値のみの超短答にする」等の代替策を検討する。
- [ ] 汎用性チェック: Q17固有の文言合わせ込みにならないよう、同種の言い換えリスクを持つ他の設問タイプ（数値・章番号・固有名詞の言い換え）にも効く設計かを確認する。
- [ ] 対策案が見つかれば1タスク1コミットで実装・step-review・valid N=3+official較正で採否判断。

**完了条件**: Q17がofficial較正でIncorrectから脱する、または汎用的な対策が見出せず「後回し」に格下げする判断を記録する。

## 3. 上記で改善余地が乏しい場合

Phase 5（`plan_0703.md` §3.5相当）へ切替:
- confidence_threshold のタイプ別グリッドサーチ
- 3時間制限の本番実測・並列化検討
- 再現性検証（seed・生成の安定性）

## 4. 保留中の小タスク

- Q79/Q92向けの新規集計answerer（test限定2問、valid該当なし）: 着手判断保留中。
- 座席表OCR（test Q13/Q46）・test Q66画像特定: 安全なMissingを維持、汎用解決策が見えるまで着手しない。
