# 次フェーズ計画（2026-07-03策定）

ベースライン提出（LB: **-0.06666**）完了後の改善フェーズ全体計画。上位戦略は [`strategy.md`](../strategy.md)、実行済みの作業は [`todo_and_experiments_baseline.md`](../todo_and_experiments_baseline.md)、データ基盤の詳細は [`data_strategy_todo.md`](../data_strategy_todo.md) / [`data_strategy/`](../data_strategy/) を参照。本書は「この後、何を・どの順で・どう進めるか」を決めるロードマップであり、各フェーズの着手時に `writing-plans` スキルで個別の実装計画（TDD・ファイル単位）に落とし込む。

---

## 1. 現状サマリ

### 1.1 スコア

| 指標 | 値 |
|---|---|
| valid 30問（ローカルjudge） | mean 0.05（Perfect 2 / Acceptable 1 / Missing 26 / Incorrect 1） |
| test 100問（LB暫定） | **-0.06666** |

- valid はほぼ全てMissingに落ちる「安全側だが得点力ゼロ」の状態。
- LBが負であることは、**test側では確信度ゲートを通過したIncorrectが複数ある**ことを意味する。守り（ゲート）にも穴がある。
- valid唯一のIncorrect（Q3・全案件の消費税合計）は、cross_project集計問題に confidence 0.55 で回答してしまったケース。現在のゲートは一律閾値0.4（`src/generator/confidence_gate.py`）で、設問タイプのリスク差を見ていない。

### 1.1.1 提出履歴（LB実測・運用ルール4）

| 提出日 | 提出物 | ローカルvalid | LB | 乖離メモ |
|---|---|---|---|---|
| 2026-07-03 | ベースライン | 0.05 | -0.06666 | ゲート素通りIncorrectで負 |
| 2026-07-04 | `submission_20260704_phase1-2fix.zip`（回答19 / Missing 81, `77c60aa`） | 0.13〜0.22（3run） | **0.03333** | 負→正に転換（守り構成の狙い通り）だが、ローカルとの乖離が大きい。0.03333=ちょうど1/30で、スコア粒度（±1/0.5）では分母100だと3.333は作れない→**公開LBはtest約100問の一部サブセット（30問前後）で採点されている可能性**。確定判断はPhase 0のjudge較正（Task 5）とtriage（Task 6-7）の結果を待つ |
| 2026-07-04 | `submission_20260704_phase2-remainder.zip`（回答20 / Missing 80, `4faa478`） | official 0.3667（`judge_calibration_1783169170.json`） | **0.03333** | **前回と完全同値（正味+1/30のまま）**。valid official が0.1667→0.3667に伸びたのにLB不変。前回比で予測が変わったのは23問（Missing→回答4: Q7/Q25/Q37/Q97、回答→Missing 3: Q42/Q52/Q76、値のみ短答化等の回答変更16）。読み: ①改善と退行がサブセット内で相殺（新規-1候補: test Q7=「条件と集計内容」型質問にoffice_style直返しがハイライト文字列で回答する型ミスマッチ、作業ログ20260704_215500で提出前から監視指定済み） ②サブセットに変更23問がほぼ含まれない引き（期待値~7問で確率は低い） ③valid→test汎化ギャップ（testの回答率20%はvalidの~43%の半分以下）。**公開LBはtest全体の一部・最終評価は残り部分（competition.md 182行）なので、1/30刻みの公開LBに過剰適合しない**。対策はLB非依存で判断できるもの優先: -1型ミスマッチのゲート強化とMissing 80問のカバレッジ |
| 2026-07-05 | `submission_20260705_pivotagg.zip`（回答20 / Missing 80, `0a0b516`） | official **0.3833**（`judge_calibration_1783182281.json`） | **0** | **前回比 net -1/30**。差分は8問のみで、実験F（pivot）はtest発火ゼロを提出前にプローブ確認済み → 犯人は実験D（Q7直返し除去）か生成ゆらぎ7問のどちらか。第一容疑は提出前から監視指定していた**Q42の値欠け不完全回答**（Missing→「…bmiの平均を集計した値」で-1化した可能性）。次点: ①Q7の直返しが実は+1だった（D仮説の反証になる） ②Q69のW6〜W7→W5〜W6 ③Q97の272消失。validはofficial 0.3833と過去最高なので、コード品質でなく**単発runの生成ゆらぎがLB粒度（1/30）で顕在化**した読み。対策はLB非依存で一般則のもの: test予測の複数run安定化（一致回答のみ採用、不一致はMissing）。**最終評価は別サブセット＋投稿2ファイル選択制（スコアの高い方が自動採用）なので、公開LB 0への過剰反応はしない** |
| 2026-07-05 | `submission_20260705_stabilized3run.zip`（回答15 / Missing 85, `a08c40a`） | official **0.4000**（`judge_calibration_1783236763.json`） | **0.13333** | **過去最高（4/30、前回比+4）**。3run多数決安定化（`beec2a4`/`b56791c`）の初回提出で、前回LB 0の容疑3問への対処（Q42値欠け遮断・Q69ゆらぎ遮断・Q97「272」回収）が狙いどおり効いた形。回答ありは20→15に減ったのにLBは0→4/30 — **不安定回答の遮断はEVプラス（誤答-1の除去 ≧ 正答+1の損失）という設計仮説をLBが裏付け**。安定化で捨てた可能性のある+1候補はQ36（0.0960/0.096の末尾ゼロ表記割れ）とQ77（3run同義言い換え割れ）— 数値同値化・記述回答の形式固定は次実験候補。以後の提出は`--runs 3`を標準とする |
| 2026-07-06 | `submission_20260706_pptxfix3run.zip`（回答16 / Missing 84, `01a0b4b`） | official **0.4667**（`judge_calibration_1783341078.json`、valid Q9 Missing→Perfect） | **0.03333** | **前回比 net -3/30**。行欠落修正＋PPTXテーブル抽出修正（version_diff PoC・本番OfficeParser両方）後の初提出。前回提出とのpredictions.csv diffは12問のみ（`docs/daily作業ログ/20260706_222657.md`）。コード起因の切り分け: 単一run診断（`diag_ssfire2_test_1783296482.json`修正前 vs `diag_pptxfix_test_1783342809.json`修正後、コード以外の条件を固定した比較）で、Q25/Q27/Q85/Q97/Q2/Q4/Q37/Q96の**retrieved_sources・回答が完全一致**（コード変更の影響なし）。唯一sourcesが変わったQ95は回答→Missingへの変化（安全側）。**→ LB低下は今回の修正起因ではなく、N=3多数決バッチ間の生成ゆらぎ（Q27は2026-07-04から既知のフリップ設問）が公開LBの小サブセット粒度で顕在化したもの**と判断。validのofficial較正（改善・new Incorrectなし）を信頼し提出は受理、次に進む |
| 2026-07-09 | `submission_20260709_kaedecontractfix3run.zip`（回答21 / Missing 79, `9dfcc5f`） | valid多数決 0.4000→0.4500（`docs/daily作業ログ/20260708_232156.md`、rankfix revert後・registry regen後の比較） | **0.16667**（=5/30） | **過去最高（前回7/5 stabilized3runの4/30から+1/30）**。`782aead`（案件名/エイリアス除去）をrevert、primary_alias再生成確認、かえで契約書を実データで復号（`literal_password_from_filename`追加）＋税区分逆順表記の金額抽出修正の3件の実データ検証・バグ修正を経た初提出。前回提出とのpredictions.csv diffは12問: Missing→回答5件（Q26/Q31/Q45/Q55/Q76、いずれもcontracts.jsonl修正やB-plan新規answererと整合）、表記ゆれのみ7件（Q6/Q8/Q23/Q27/Q37/Q48/Q60、意味的に同一）。Q38（APR-M3集計）は`データアステル社内管理_決裁基準.md`と突き合わせ、現在の実データでは該当0件が正しいためMissing維持を確認。Q79はanswerer自体が別ブロック扱いで未実装のためMissing据え置き。**5/30ちょうどという値は「公開LBはtest約30問前後のサブセット・刻み幅1/30」という従来仮説と整合**。当て推量ではなく実データ検証済みのバグ修正（decrypt・regex）による回収5問のうち、少なくとも1問が公開サブセット内で正答化したとみられる |
| 2026-07-12 | `submission_20260712_analysis-pwfix3run.zip`（回答34 / Missing 66, `4b74e04`、3-run安定化） | Phase4 analysisブロックのvalid official較正は**0.6333**（`judge_calibration_1783848948.json`） | **0.16667**（=5/30） | **過去最高に並ぶ（7/9 kaedecontractfix3runの5/30と同値。直前のphase4-imagegraph提出3/30からnet +2/30の回復）**。前回（phase4-imagegraph）提出との差分は7問。うち6問（Q4/Q5/Q32/Q35/Q61/Q73）はanalysisブロックの狙い通りの回収（Missing→`structured:analysis`の決定的回答。Q4は誤答"age"→正しい"bmi"に修正、target 5問はvalid officialで既にPerfect 5/5確認済み）。Q69（Missing→W5〜W6）は2026-07-05から既知の生成ゆらぎで今回の変更とは無関係、プラス・マイナス両方向のノイズ源。パスワード復号配線（`6692190`/`ea70afe`）が対象と見込んだQ79/Q92は該当answerer未実装のためMissing継続で差分なし（想定通り、新規リスクなし）。**+2/30の回復はanalysisブロックの決定的回答6問のうち少なくとも一部が公開サブセット内で正答化したことと整合**するが、GT非公開のため個別問題の寄与は確定できない |
| 2026-07-12 | `submission_20260712_phase4-imagegraph.zip`（回答28 / Missing 72, `7a60490`） | valid official **0.5333**（`judge_calibration_1783838926.json`、新規Incorrectなし・既知Q17のみ） | **0.1**（=3/30） | **前回比 net -2/30（過去最高5/30から後退）**。Phase4 image_graphブロック（ネイティブチャートXML抽出・VLM接続）を初めて反映した提出。前回とのpredictions.csv diffは9問（実質8問、Q48は表記ゆれのみ）: Q33/Q39/Q54はimage_graphのネイティブチャート抽出（3run unanimous・confidence 0.9、事前算出済み決定値`hum`/`0.19555`/`0.50239`と一致確認済み）でおそらく正答側。Q25（ハイライト合計値`-11850477`→`-11850576`）はPhase4以前の既存バグ修正`f99324f`（2026-07-10検証済み）の反映であり本ブロック起因ではないと裏取り済み。**第一容疑は今回初めて実データで発火した非決定的パス4問**——Q9（office_style自由記述、confidence 0.5）・Q40（payment_schedule集計、confidence 0.8）・Q87（contract_apr判定、confidence 0.85）・Q94（該当なし、confidence 0.5）——のいずれかがtest実データで誤りだった可能性（いずれもPhase3で実装済みだが今回のrunで初めてMissingを脱した設問で、GTなしのため単体では正誤確認不可）。validはofficial 0.5333で新規Incorrectなし・過去最高のため、コード品質全般の後退ではなく個別設問の生成結果とサブセット粒度（1/30）が主因と判断。**最終評価は別サブセット＋2ファイル選択制のため過剰反応はしない**が、Q9/Q40/Q87/Q94は次回提出前に実データで裏取りする候補として控える |
| 2026-07-13 | `submission_20260713_docscope3run.zip`（回答32 / Missing 68, `009800c`、3-run安定化） | docscope（`f59c732`）のvalid official較正は**0.6000**（`judge_calibration_1783945810.json`、新規Incorrectなし・既知Q17のみ、Q28はjudge非決定性と確認） | **0.2**（=6/30） | **過去最高更新（前回0.16667=5/30からnet +1/30）**。前回`analysis-pwfix3run`提出とのpredictions.csv diffは8問。うちQ75（Missing→「4週目」）はdocscope（named-file boosting）の狙い通りの回収候補。Q43（"石川直樹"→"石川 直樹"）・Q60（列挙の順序のみ変化、要素集合は同一）は表記ゆれで意味は不変。Q8（"約14,744ドル"→Missing）・Q9（説明文→Missing）・Q69（"W5〜W6"→Missing）・Q94（"該当なし"→Missing）は既知の非決定的パス（`plan_0703.md`前回行で言及したQ9/Q94含む）が今回Missing側に倒れた——安全方向のノイズ。Q97（Missing→"272"）は2026-07-05から既知のフリップ設問。**+1/30はdocscopeの決定的改善（Q75等）が非決定パスのMissing化と相殺しつつネットでプラスに振れたことと整合**するが、GT非公開のため個別問題の寄与は確定できない。既知Incorrectパターンは引き続きQ17のみ（Q18系は本提出で解消済み） |
| 2026-07-15 | `submission_20260715_hybridsearch3run.zip`（回答35 / Missing 65, `047ccd1`、3-run安定化） | ハイブリッド検索（BM25+ベクトルRRF）のvalid official較正（r1）は**0.7333**（`judge_calibration_1784040090.json`、新規Incorrectは検索成功の副作用のQ6のみ・既知Q17は継続） | **0.2**（=6/30、前回docscope3runと同値） | **前回比 net 0（横ばい）**。前回`docscope3run`提出とのpredictions.csv diffは6問のみ、いずれも3run間で多数決一致（majority/unanimous、一発性のゆらぎではない）: Q27「5項目→6項目」（値の精緻化）、Q41/Q59/Q69/Q85がMissing→回答（検索改善によるカバレッジ回復）、Q96が回答→Missing（3run全会一致の撤退、CRAG採点上は安全方向）。valid official mean は0.6000→0.7333と明確に改善したにもかかわらずLBは横ばい——**[[lb-granularity-and-valid-mismatch]]と整合する既知の乖離パターン**: 公開LBは約30問の固定サブセットを1/30刻みで採点しており、今回の変更で動いた6問（test全100問中）がそのサブセットにほぼ含まれなかった可能性が高い（差分6問がサブセットに1問も入らない確率は無視できない水準）。ハイブリッド検索自体はvalid/official・retrieval recallの両方で明確な改善が実測されているため、**LB横ばいをもって後退とは判断しない**。最終評価は別サブセット（かつ2ファイル選択制）のため、valid/official側の実測改善を信頼して採用を継続する |
| 2026-07-16 | `submission_20260716_q17-filescope3run.zip`（回答34 / Missing 66, `2ed2dcb`、3-run安定化） | Q17 filescopeのvalid officialは**0.8000**（直前0.7333から+0.0667、Q17 Incorrect→Perfect、新規Incorrect 0） | **0.23333333333333334**（=7/30） | **過去最高更新、前回0.2からnet +1/30**。前回提出とのpredictions.csv diffは9問：Missing→回答 Q8/Q9/Q19、回答→Missing Q27/Q41/Q97、意味不変の表記・順序差 Q60/Q69/Q85。Q17対策による検索recall 0.793→0.862、validの+2/30と同方向で、testでも拡張子なし名指しファイル回収が少なくとも1問分のnet改善に寄与した可能性と整合。GT非公開のため個別寄与は断定しないが、採用判断をLBも支持 |

### 1.2 すでに持っている資産（未接続）

ベースラインとは別トラックで、データ戦略側の構造化資産が `artifacts/` に構築済み。**これらはまだ回答パイプラインに1つも接続されていない**。ここが最大の伸びしろ。

| 資産 | 内容 | 効く設問タイプ |
|---|---|---|
| `project_registry.json` / `term_registry.json` | 案件・略称（KSS, AYM, TG, PP, CT…）・用語の辞書 | internal_terms 11問＋全タイプのルーティング精度 |
| `document_registry.jsonl` | 416ファイルの案件・セクション・版タグ | 全タイプ |
| `spreadsheet_cells.jsonl` / `highlight_cells.jsonl` / `train_xlsx_*` / `schedule_tasks.jsonl` | セル値・数式・色・フィルター・Pivot | spreadsheet_state 21問 / spreadsheet_calc 9問 |
| `office_marks.jsonl` | 太字・下線・イタリック・ハイライト・色 | office_style 9問 |
| `version_pairs.jsonl` / `version_diff_poc.jsonl` | 全11ペアの行単位diff生成済み | version_diff 11問 |
| `encrypted_file_queue.jsonl` | 暗号化/旧形式ファイル2件の特定 | パスワード設問 |

### 1.3 設問タイプ分布（`question_labels.csv`、test 100問の primary_type）

| タイプ | test | valid | 資産の準備度 |
|---|---:|---:|---|
| spreadsheet_state | 21 | 3 | ◎ 抽出済み・接続待ち |
| version_diff | 11 | 1 | ◎ diff生成済み・意味分類が未実装 |
| internal_terms | 11 | 4 | ◎ term_registry 構築済み |
| contract_rule | 10 | 3 | △ 契約書テキストはあるが計算パスなし |
| office_style | 9 | 3 | ◎ 抽出済み・接続待ち |
| spreadsheet_calc | 9 | 3 | △ セルデータあり・計算エンジンなし |
| cross_project | 9 | 1 | △ registryあり・横断集計テーブルなし |
| single_text | 6 | 4 | ○ ベースラインの守備範囲のはずが取れていない |
| list_extraction | 4 | 2 | ○ 同上（＋全要素一致ゲートが必要） |
| analysis_metrics | 3 | 1 | △ |
| notebook_output | 2 | 2 | ○ パーサーあり |
| image_graph | 2 | 1 | ✕ VLM未実装 |
| code_static | 2 | 2 | ○ パーサーあり |
| numeric_exact | 1 | 0 | - |

> **優先度の修正**: `todo_and_experiments_baseline.md` のバックログでは「1. 画像/グラフのVLM化＝優先度最高」としていたが、ラベリング結果では image_graph が primary の設問は **testで2問のみ**（secondaryを含めても少数）。一方 spreadsheet_state だけで21問ある。**VLM化はPhase 4に降格し、Excel/Office構造化資産の接続を最優先とする。**

---

## 2. 診断: スコア0.05の構造と、最初に解くべき問い

Missing 26問は一枚岩ではない。改善に着手する前に、各Missingを次の3類型に切り分ける必要がある（strategy.md §6-3）:

1. **検索の失敗** — 正解を含むチャンクがtop-kに入っていない（例: valid Q5 は最終報告書pptxの表紙スライドしか取れていない → チャンク化・検索の問題）
2. **生成の失敗** — 根拠はコンテキストにあるのにLLMが答えない/間違える
3. **較正の失敗** — 正しく答えられたのにゲートで落ちた（confidence が一律に0.0〜0.2に張り付いている疑い）、または answered すべきでないのに通過した（valid Q3、LBのIncorrect群）

現状はこの切り分けを機械的に行う手段（retrieval recall 評価）がない。**Phase 0 でこの計測基盤を先に作る**。感覚でバックログを消化しない。

### 2.1 実測結果（2026-07-04, Phase 0 完了時点）

切り分け表: [`missing_triage_20260704.md`](missing_triage_20260704.md)（run: `phase0_valid_1783125252.json` mean 0.15, P5/A1/M23/I1）

| 分類 | 件数 | 対応フェーズ |
|---|---|---|
| 検索失敗 | 10 | Phase 1〜（チャンク・検索の修正。§5系） |
| 生成失敗 | 12 | Phase 1〜（生成プロンプト・コンテキスト整形） |
| 較正失敗(過剰ゲート) | 1（Q18） | フリップ層の安定化（next-steps §2） |
| 較正失敗(ゲート素通り) | 0 | — |
| 計測不能(手動確認) | 1（Q3 cross_project） | 個別確認 |
| OK | 6 | — |

- **retrieval recall（計測可能29問, top-5）: 66%（19/29）**。全滅タイプ: `spreadsheet_state` 0/3・`code_static` 0/2、弱い: `spreadsheet_calc` 1/3・`office_style` 2/3（タイプ別詳細は `experiments/retrieval_valid_1783123484.json`）。※初回計測はNFC/NFD突合バグでrecall 0と誤計上→修正済み（`68fc097`）
- **judge較正: official (gpt-5.2-2025-12-11・本番同一プロンプト) mean 0.1333 vs local (claude) 0.1500、一致率 90%（27/30）**。乖離3件: Q14 Acceptable→Missing / Q17 Missing→Incorrect / Q27 Missing→Perfect（`experiments/judge_calibration_1783125320.json`）。**LB乖離の解釈: judge差は±0.02程度で、LB 0.03333とローカル0.13〜0.22の乖離はjudgeでは説明できない** → 公開LBのサブセット採点（0.03333=1/30、§1.1.1）か、test固有の失敗が主因
- 実験サイクル実測: 30問フル評価 **2分3秒**（インデックス構築62秒＋30問生成/judge 59秒）。トークン: 生成 in 69.5k / out 11.7k、judge in 10.7k / out 3.8k（いずれもsonnet系。単価改定に備え$換算でなくトークンで管理）。retrieval recall評価は76秒・LLM不使用で無料
- **Phase 1 優先度への影響**: 当初仮説「過剰ゲートが主因」は否定された（過剰ゲートは1問のみ。confidenceの張り付きはPhase 1是正で解消済みとみられる）。現在の主因は生成失敗12・検索失敗10の二本柱。検索失敗はspreadsheet系に集中しており、next-steps §3のspreadsheet_state/calc対策（構造化パス接続）がそのまま検索失敗対策を兼ねる。生成失敗12問はraw_answer付きで記録されているので、次サイクルで個別に原因を読む

### 2.2 フリップ層安定化の実測（2026-07-04, 実装計画: `../superpowers/plans/2026-07-04-flip-stabilization.md`）

基盤整備: gate_reason記録（`589d6bf`）・N=3多数決評価ツール（`eb2e785`、ベースライン多数決mean 0.1500・不安定問Q5/Q14）。

| 実験 | 変更 | 多数決mean | 主要な観測 | 判定 |
|---|---|---|---|---|
| ① | JSONパース`strict=False` | 0.15→0.15 | Q18を1/3runで回収（conf 0.55パース成功を実証）。残2runは別要因のJSON破損 | **採用**（`89435eb`） |
| ② | 引用30字以内・省略記号禁止 | 0.15→**0.10** | citationゲート死亡は3run全て0件になったが、Q18多数決がMissing→**Incorrect**（ゲート通過誤答） | **不採用・revert**（証跡`f417aec`） |
| ③ | 短答系は文書表記のまま冒頭直答 | 0.15→0.1333※ | 不安定問6→3・3run全てlocal Incorrectゼロ（Q28のIncorrect発話停止）。※mean減はlocal judgeの正答→Missing誤判定（Q8はofficial Perfect、raw検証済み） | **採用**（`641e858`） |

**学び（以後の実験運用に直結）**:
1. **引用ゲートには誤答遮断の防御価値がある**。偽陰性（正答を殺す）コストより大きい。引用を緩めるとIncorrectが素通りする（実験②の負の結果）
2. **local judge（claude）は正答をMissingに誤判定する率が無視できない**（Q5/Q8/Q17/Q27で確認。official較正: Q8官Perfect・Q27官Acceptable）。**mean単独で実験の採否を判定しない** — 多数決＋不安定問一覧＋raw_answer読み＋必要ならofficial較正で判断する
3. **新リスク: ゲートを通したQ18がofficial Incorrect**（正解「3」に対し「3章です。…」の説明付き回答が-1化、`judge_calibration_1783140909.json`）。数値・章番号系の正解は**「値のみ」の超短答**にする実験が次候補。Q17（「未連絡」を「前回未接触」と言い換え）も実験③では未解決で残存 — この2問はゲート通過中の実弾-1リスク
4. run間ゆらぎの実態: 30問meanは同一コードでも0.10〜0.22で振れる。うち相当部分がjudgeゆらぎ（パイプライン出力自体は安定して正答のケースあり）

### 2.3 短答値のみ＋spreadsheet_state強化の実測（2026-07-04, 計画: `../superpowers/plans/2026-07-04-value-only-and-spreadsheet-state.md`）

基盤: スキャナ拡張（`ab7b427`: autoFilter条件そのもの・非表示行数・Pivot等小型シートの全セル値14,803行をartifacts化）。

| 実験 | 変更 | 判定 | 実測 |
|---|---|---|---|
| A | 短答answerは値のみ（説明はreasoningへ） | **採用**（`bc22924`） | local多数決 0.1333→0.15、**official 0.1167→0.1667**。Q8「1,168,750円」Q27「0.010301」を完全一致回収 |
| B | フィルタ条件文＋Pivot argmax行の構造化接続 | **採用**（`621d063`） | **local多数決 0.15→0.2833**（悪化0・Incorrect 0）。Q11がフィルタ条件を3/3 Perfect |
| B2 | calc分岐fall-through＋poolカウント修正＋compact階層pivotのabstainガード | **採用**（`e212126`） | Q6回収（local Perfect 3/3）・Q21はcompact階層のため保守的にMissing。多数決0.2833維持。official較正(B2b run) **0.2333** |
| B3 | Q6のofficial不一致対策（正解文型の合成テンプレート＋LLM非経由の直接回答） | **不採用・全revert（規約リスク）** | step-reviewがCONFIRMED指摘: テンプレートの接尾句がQ6の正解文からの逆算で、同型のQ11/Q21の正解には現れない → competition.md「個別設問の正解を回答生成用ルールに反映」への抵触と判断。run JSONは証跡保存のみ |
| C | Phase 2残課題: office_style誤ルーティング、calc group_by/list、office_style直返し | **採用**（`8b07c3a` / `725acfd` / `37e62f1` / `ef22058`） | Q7はlocal/official Perfect（`judge_calibration_1783163986.json`でQ7 official Perfect）。Q26は単発official Perfect（`train_0077、train_0216、train_0242、train_0722`）。Q25は単発スモークで `1. データ理解・EDA` 回収。Q0はcapability gateを脱出しmissing_textまで進むが、文脈不足で未回収。以後の検証は対象問スモーク→official単発→必要時N=3に縮小 |
| C検証 | HEAD（ef22058）最終構成のN=3＋official較正（後追い検証。C実装時はef22058後のvalid runが無かった） | **採用確定**（`eaf6f77`） | `exp_phase2rem_head_valid` N=3: local多数決 0.3333→0.3000だが低下分はjudge誤判定（Q25はGT完全一致「1. データ理解・EDA」を3/3 Incorrect判定、Q27は同一回答'0.010301'にP/M/A）。**official較正 `judge_calibration_1783169170.json`: mean 0.2667→0.3667**、Q25/Q26/Q27 official Perfect、Q23はAcceptable 3/3に安定化（前回A/M/M）、official IncorrectはQ6が解消しQ17のみ（2→1） |
| D | office_style直返しの抽出意図ゲート（LB乖離分析で発見したtest Q7型ミスマッチ対策: 「条件・集計内容を答えて」型は直返ししない、2026-07-05） | **採用**（`869a1ff`） | valid 30問の回答はC構成と**全問バイト同一**（validに無影響が設計意図。効くのはtest Q7型のみ）。local多数決0.3500・official 0.3333（`judge_calibration_1783177178.json`）。official IncorrectのQ6復活は同一回答への判定ゆらぎ（較正4回中3回Incorrect、C検証較正のみPerfect — §2.3学び3の実証）で変更無関係 |
| E | カバレッジブロック: chunker境界スナップ＋案件名トークン除去＋ファイル名ヒント（Codex実装 `6eb8c5f`、3変更同居） | **不採用・全revert**（`df23b2a` / `b6c9a66`。src/は42c73d2と同一に復帰） | フル構成: Q12回収（¥5,775,000 Perfect 2/3）だが**Q18が「3」→「4」で3/3 Incorrect**・Q27桁化け（0.010301→0.010310）。LLM不使用の検索プローブ3構成比較で切り分け: Q18はchunker由来、**Q27はretriever由来**、Q12は両変更の合成が必要。retriever単独構成もQ14 A→M 3/3・Q27残存で利得なし。証跡: `exp_covblock_valid`×3・`exp_covblock2_valid`×3 |
| F | Q21型compact pivot回収: pivotTable XML＋pivotCache再集計の構造化パス（Codex実装 `b265b12`、2026-07-05レビュー承認） | **採用** | `exp_pivotagg_valid`×3: **Q21がMissing→Perfect 3/3**（決定的コンテキスト＝回答も3run同一）。local多数決 0.3500→0.3333だが低下分（Q17/Q26）は6run全て回答バイト同一のjudgeゆらぎ。コンテキスト変化はQ6/Q21の2問のみ（`retrieved_sources`突合。Q6は新パス発火で表現が`Gender = Male、disease = 1、Age = 68`に変化、local Perfect 3/3維持）。**official較正 `judge_calibration_1783182281.json`: mean 0.3833（過去最高）**、Q21/Q11 official Perfect、official IncorrectはQ17のみで不変。Q6はofficial Missing（学び3のbrittleness。D較正のIncorrect=-1より改善方向） |
| G | chunker境界スナップをコード系ファイル（`.py`/`.ipynb`）限定にし、Officeドキュメントは固定幅チャンクに戻す（Eで「Q18はchunker由来」と特定された退行の再修正、`2d6781d`） | **不採用・revert**（`6f745c4`） | `git worktree`で直前コミット（`f99324f`）をチェックアウトしN=3 vs N=3の厳密なA/B（`prechunker_valid`×3 vs `chunkerfix_valid`×3、worktree産のためrun JSONは非保存・本ログとdocs/daily作業ログ/20260710_173153.mdが一次情報）: mean多数決 0.3167→0.3500。Q18/Q27の「改善」はIncorrect→Missingへの付け替えのみで実質不安定なまま。**Q12「京橋信用ソリューションズの契約金額」が3/3 Perfect（`5,775,000円`）→3/3 Missingへ確定的に退行**（`retrieved_sources`が`契約書.docx::full_chunk1`→`full_chunk8`に飛び、固定幅化で金額の数値トークンがチャンク境界で分断されたとみられる）。Eの記録で「Q12回収にはchunker+他変更の合成が必要」と示唆されていた通り、Q12はchunkerの境界位置に敏感な設問と再確認。行境界スナップは境界安定化だけでなく数値トークンの泣き別れ防止も担っており、Office文書での単純除去は不可 |

**スコア推移（2026-07-04の1日）**: local多数決 0.1333 → 0.3333（C後の生run群では0.3000だがjudge誤判定含み）、official単発 0.1167 → **0.3667**（朝のPhase 0時点はofficial 0.1333）。

**学び**:
1. **評価専用のGT参照と生成側への知見反映の境界を実地で確認**: judge較正・triageはGT利用が適法だが、**正解文の表現・型を生成側テンプレートに写すのは違反側**（レビューが停止させた。エージェントの自走でも越えかけた線 — 以後、生成側の変更は「未知の質問でも同じ導出になるか」に加えて「この形はどの実測から来たか」を自問する）
2. LLMは値のみルール下で回答の尾部（対象指標など）を刈る。Q6型（記述文GT）のofficial回収は未解決課題として残る — 規約適合の範囲での一般的な回答形改善（例: 条件を問う質問には条件＋対象を含める汎用ルール）は将来実験の候補
3. Q6のofficial判定は表現に極端に敏感（完全一致=Perfect、区切り文字違いでMissing/Incorrect）。この判定brittlenessはLB乖離の一因になりうる
4. **local judgeはMissing誤判定だけでなくIncorrect誤判定もする**（Q25: GT完全一致の回答を「参考文書と無関係」として3/3 Incorrect化 — 検索文脈から妥当性を推測するためGT一致でも殺す）。localのIncorrect増を見たら、まずraw_answerとGTの目視 → official較正で確定。判定原則2（Incorrect増は拒否）の適用は「officialのIncorrect増」で判断する
5. **検索レイヤ（chunker/ランキング）の変更は-1製造機になりうる**（実験E）: 回収狙いの問だけでなく「現在正答している問のコンテキスト」も全て入れ替わるため、Q18型（正答→別チャンクの誤読で-1）・Q27型（数値の桁化け）が発生する。チャンク割り変更は`full_chunkN`の中身が全ファイルで変わる=影響範囲が全問。今後この層を触るなら「既存OK問のtop-5を変えない」no-harm制約を設計に含めること。また**複数変更の同居はプローブ（LLM不使用のtop-5比較）で安価にbisectできる**（今回Q27の真犯人をretrieverと特定できたのはこれ）

**残課題（優先順・2026-07-05更新）**: ①Q17/Q18のゲート通過中-1リスク ②Q6のofficial表現問題（実験Fで区切り文字が変わりofficial Incorrect→Missingに遷移。GT形式への合わせ込みは規約違反のため放置が正） ③カバレッジ攻めの再設計（no-harm制約付き。Q12は「案件名除去＋チャンク改善の合成」で取れることが実験Eで判明済み） ④pivot多dataField案件（かえで8・ひがし丘7・青潮8）で日本語指標名質問はterm_registry未接続のため発火しない — 必要になったらPipelineからstructured contextへ展開済み質問を渡す設計変更（実験F作業ログ参照） / **クローズ**: Q21型compact pivot（実験Fで回収・official Perfect）。Q0・Q10はラスタPDFで能力外と確定（`docs/pdf_highlight_feasibility.md`）

---

## 3. フェーズ計画

原則（strategy.md §6）: 守りが攻めに先行 / 期待値 = 該当問数 × 改善確率 × 得点差 / 1実験1変更・flip分析 / validへの過剰適合回避。

### Phase 0: 計測基盤（最優先・短期）

**目的**: 以降の全実験の「効いた/効かない/なぜ」を数分で判定できる状態を作る。

- [ ] **検索単体評価（retrieval recall）**: 質問ごとに「正解根拠ファイル（`question_labels.csv` の source_section と案件から機械的に絞った候補）がtop-kに入ったか」を出すスクリプト。`scripts/eval_retrieval.py`
- [ ] **Missing 26問の失敗切り分け**: 上記を使い、valid全問を「検索失敗 / 生成失敗 / 較正失敗」に分類してこの文書に追記。以降の優先度をこの実測で更新する
- [ ] **flip分析の標準化**: `scripts/run_eval.py` に「前回runとの差分（改善した問題/悪化した問題）」出力を追加
- [ ] **実験サイクル高速化**: パース・インデックス結果のキャッシュ（変更がない限り再ビルドしない）。30問評価1回の所要時間とAPIコストを記録
- [ ] **judge較正**: OpenAIキーが使えるなら valid 30問を `evaluation/crag.py`（gpt-5.2系）でも採点し、ローカルClaude judgeとの乖離表を1回作る（LB -0.067 とローカル 0.05 の乖離解釈に必須）

**完了条件**: 「変更→30問評価→flip確認」が数分＋既知コストで回る。Missing 26問の切り分け表が存在する。

### Phase 1: 守りの再設計と土台修正（ベースラインの取りこぼし回収）

#### Phase 1 実装結果（2026-07-03、Codexにより実装）

run: `experiments/phase1_valid_1783076647.json`（比較対象: `experiments/baseline_valid_1783069301.json`）

| 指標 | ベースライン | Phase 1後 |
|---|---:|---:|
| mean_score | 0.05 | 0.00 |
| Perfect | 2 | 1 |
| Acceptable | 1 | 0 |
| Missing | 26 | 28 |
| Incorrect | 1 | 1 |

- 実装内容: タイプ別確信度ゲート／根拠引用の実在チェック／案件エイリアス・社内用語レジストリのクエリ接続／生成プロンプト調整
- 未着手: チャンク・検索の修正（Phase 0の検索失敗切り分け結果待ち）
- 判定: Incorrect は増えていないが、mean_score が 0.05 から 0.00 に悪化したため、Phase 1完了条件は未達。根拠引用チェックまたは能力外・高リスクゲートで取れていた回答までMissing化している可能性があるため、flip分析で悪化問題を確認して調整が必要。
- **→ 2026-07-04 是正済み**: flip分析の結果、悪化2問は (a) 引用チェックの空白揺れ偽陰性・「わかりません」部分回答の過剰ゲート、(b) 生成LLMのconfidence自己申告の崩壊が原因。(a)はコード修正、(b)はPhase 2構造化パスで正しいコンテキストを渡すことで解消（下記Phase 2実装結果参照）。

#### Phase 1+2 是正後の実装結果（2026-07-04、レビュー修正により実装）

run: `experiments/phase2fix_valid_1783092014.json` ほか計3run（比較対象: `experiments/baseline_valid_1783069301.json`）

| 指標 | ベースライン | Phase 1後 | 是正後（3runの範囲） |
|---|---:|---:|---:|
| mean_score | 0.05 | 0.00 | **0.13〜0.22** |
| Perfect | 2 | 1 | 4〜6 |
| Acceptable | 1 | 0 | 0〜2 |
| Missing | 26 | 28 | 23〜24 |
| Incorrect | 1 | 1 | 0〜1 |

- 主な是正内容:
  - **dispatcherのNFDパスバグ修正（最重要）**: 「プロジェクト」判定がNFCリテラル比較だったため、実データで全ドキュメントのprojectメタデータがNoneになり、案件スコープ検索・案件検出・Phase 2構造化ルーティングが一度も機能していなかった。
  - Phase 1過剰ゲート緩和（引用チェックの空白正規化、部分回答の誤Missing化修正）
  - Phase 2ルーティング修正（office_style/spreadsheet_stateのフォールスルー化、schedule_tasksマッチングの実データ表記対応、spreadsheet_calcのゲート時フォールバック・groupby拒否・別案件train.csv禁止、列挙完全性ゲートの実質化、office_styleのdocxハイライト対応・質問ヒント絞り込み・コンテキスト自己記述化）
- 効果例: valid Q20（AYMタスクID列挙）がschedule_tasksから決定的に回答されPerfect復帰。Q13（spreadsheet_calc）がpandas集計でPerfect化。Q3（全案件横断の消費税集計）はmulti_hopゲートでIncorrect→Missing。
- **既知の制約: 同一コードでもrun間でmean_scoreが±0.05程度ゆらぐ**（claude-sonnet-5はtemperature指定不可＝API 400。同一回答文でjudgeラベルが反転する例も確認）。単一runでの小差比較は不可。今後の実験比較は複数run or フリップ分析で行うこと。
- 残課題（Phase 2完了条件「該当タイプでPerfect過半」は未達、対象9問中Perfect 1）:
  - office_style: 「M02資料」→`報告資料_2025-08-06.docx`のようなマイルストーン番号→ファイルの対応付けが無くLLMが確信を持てない（Q0/Q23/Q25）
  - spreadsheet_state: フィルタ条件質問はヘッダ＋非表示行番号だけでは条件を導けない。可視行/非表示行の値差分の実装が必要（Q11）。Pivot集計質問はtrain_xlsx系artifactsの接続強化が必要（Q6/Q21）
  - spreadsheet_calc: groupby/argmax系（Q7/Q26）は安全のため意図的にMissing（CalcSpec拡張で対応可能）
  - 通常パス: notebookのチャンク分割で判定条件が泣き別れて部分コードから誤断定（Q28、唯一のIncorrect）→「チャンク・検索の修正」スコープ

**目的**: (a) LBを負にしているIncorrectを構造的に塞ぐ、(b) 現能力で取れるはずの low-risk 設問（valid 8問 / test 16問）をPerfectにする。

守り:

- [ ] **タイプ別確信度ゲート**: `question_classifier.py` の分類を使い、リスク別に閾値を変える（数値・列挙・cross_project は閾値引き上げ。能力外タイプ — image_graph、未実装の集計系 — は問答無用でMissing）。CRAG非対称性（Incorrect=-1）への直接対策
- [ ] **根拠引用の強制**: 生成時に「使用チャンクID・引用文」を出させ、引用がコンテキストに実在しない回答はゲートで落とす（ハルシネーション機械検出）

攻め（土台）:

- [ ] **term/project registry のクエリ処理接続**: 質問中の略称（KSS, AYM, TOTO, MINAMINO…）・社内用語（TG, PP, CT, FR, ESTH…）を registry で展開してから案件ルーティング＋検索する。ベースラインの既知の穴（略号での言及が全体BM25フォールバック頼み）を塞ぐ。internal_terms 11問だけでなく全タイプの検索精度の土台
- [ ] **チャンク・検索の修正**: Phase 0 の切り分けで「検索失敗」と判定された問題を対象に、pptx/docxのスライド・見出し単位チャンク化、top-k、メタデータ（document_registry 由来の document_type / version_tag）付きフィルタリングを調整。ベクトル検索＋ハイブリッド化はここで効果を測ってから採否を決める（recall が BM25 改善だけで足りるなら見送り）
- [ ] **生成プロンプト調整**: 「わかりません」に逃げすぎる傾向（valid Q5: 根拠が表紙しかないのに confidence 0.9 で「わかりません」と答える等、confidence の意味が壊れているケース）の修正。confidence の定義をプロンプトで固定する

**完了条件**: valid の single_text / code_static / notebook_output / list_extraction（低リスク帯 約10問）で Perfect が過半。valid Incorrect 0 を維持。retrieval recall（該当タイプ）80%以上。
**期待値**: test の low-risk 16問 ＋ internal_terms の一部 → LB を +0.1〜0.2 帯に乗せる。

### Phase 2: 構造化資産の接続（最大の山: Excel/Office 系 39問）

**目的**: spreadsheet_state 21問・office_style 9問・spreadsheet_calc 9問を、既存 artifacts を使った**専用回答パス**で取る。

- [x] **ルーティング**: 質問分類（`question_classifier.py`）で spreadsheet/office 系と判定された質問を、チャンク検索ではなく構造化パスへ振り分けるオーケストレーション変更（`src/orchestrator/pipeline.py`）。実装は7/4時点で完了しているが、本書のチェックボックスが未更新のまま放置されていた（下記2026-07-11実測で判明）
- [x] **spreadsheet_state パス**: `highlight_cells` / `train_xlsx_*` / `schedule_tasks` / フィルター・Pivot情報からの回答生成を実装済み
- [x] **office_style パス**: `office_marks` から太字・下線・イタリック・ハイライト・色の該当箇所を列挙して回答。実装済み
- [x] **spreadsheet_calc パス**: train.csv/xlsx への NL→pandas 集計を実装済み（実行失敗時はMissingへフォールバック）
- [x] 列挙系の完全性ゲートも実装済み（`gate_reason`で確認可能）

**完了条件**: valid の spreadsheet_state 3問・office_style 3問・spreadsheet_calc 3問で Perfect 過半、Incorrect 0 → **達成**（下記2026-07-11実測）。
**期待値**: 該当39問 × 成功率5〜6割 → **+0.2 以上**。本コンペ最大の単一投資。

#### Phase 2 実測結果（2026-07-11、既存実装の現状確認）

**背景**: 本書のPhase 2チェックボックスは全て未着手のまま放置されていたが、`pipeline.py`を確認すると実装自体は7/4〜7/6に既に完了しており、`plan_0703.md`側の更新漏れだった（cross_projectブロック完了を機にPhase2の現状を再確認）。

**手順上の注意（今後のため記録）**: `run_pipeline.py --data-dir`のデフォルトは`data/sample`（ダミーデータ）。実データ診断には`--data-dir "data/raw/share/共有ドライブ" --questions "data/raw/share/質問回答/questions_valid.csv"`を明示する必要がある（指定漏れで一度、全問Missing近くの誤った結果を得て「壊滅的回帰」と誤認しかけた。メモリ`pipeline-data-dir-default-trap`に記録済み）。

valid診断run×3（`phase2_recheck_valid_*`、正しいdata-dir）を実施、うち1runを`calibrate_judge.py`でofficial較正（`judge_calibration_1783777064.json`、official mean **0.4833**、local mean 0.4500、一致率80%）:

| タイプ | 該当問（valid） | local judge | official judge |
|---|---|---|---|
| spreadsheet_state | Q6/Q11/Q20/Q21/Q23（5問） | Q11/Q20/Q21 Perfect安定、Q23 Acceptable安定、Q6不安定(Missing/Acceptable) | Q11/Q20/Q21 Perfect、**Q23 Perfect**（local Acceptableから上振れ）、Q6 Missing |
| office_style | Q0/Q23/Q25（3問） | Q0 Missing安定（画像のみ・VLM待ち、安全）、Q23 Acceptable安定、Q25 **Incorrect安定** | Q23 Perfect、**Q25 Perfect**（local Incorrectはjudge誤判定と判明、下記） |
| spreadsheet_calc | Q1/Q6/Q7/Q13/Q21/Q26（6問） | Q1 Missing安定（image_or_graph併発・VLM待ち）、Q7/Q13/Q21 Perfect安定、Q26 Perfectだが1回Acceptable | 個別較正未実施だがQ7/Q13/Q21はlocal Perfectで一致率80%の範囲内、大きな乖離候補ではない |

**Q25「local Incorrect」はパイプラインのバグではなくlocal judgeの誤判定と判明**: 東都提案書P7の「赤で強調されている箇所」問題で、回答「1. データ理解・EDA」は`office_marks.jsonl`実データで唯一`fill_color: A23B2C`（他4項目は`3D3D3D`グレー）を持つ正しい抽出結果。official judgeはPerfectと判定しており、local judgeが「参考文書が無関係」と誤って評価していた。**コード修正は不要**（strategy.md §2.3「local judgeはIncorrect誤判定もする」の実例）。

**Phase 2完了条件は事実上達成**: 対象9問（spreadsheet_state 3・office_style 3・spreadsheet_calc 3のサンプル要件）どころか、実際の対象14問中Perfect/Acceptableが大半、Incorrectはofficial判定で0件。残るギャップはQ0/Q1（office_style/spreadsheet_calc双方でimage_or_graph併発、座席表と同型のVLM待ち課題）とQ6の不安定性（要因未調査、優先度低）のみ。

**このrunで新たに発覚したofficial Incorrect**: Q17（`京橋のpdays=-1の意味`、GT「未連絡」に対し回答「前回未接触（顧客に一度も接触していないこと）を示す特別値」）。意味は一致するが冗長な言い換えがofficial judgeにIncorrect判定されている。plan_0703.md既知のQ17パターン（§2.3「Q6のofficial判定は表現に極端に敏感」と同型）の再確認。strategy.md §3.3「簡潔な回答のほうがjudgeの誤判定も減る」の生成プロンプト調整（GTの粒度・簡潔さに合わせる）が汎用的な対策候補（valid 1問限定の対症療法ではなく、text_only/internal_terms全体に効く可能性）。

### Phase 3: 差分・契約・横断（30問）

Phase 2 と一部並行可（独立領域のため）。

- [x] **version_diff（11問）**: `build_version_diff_context`で回答パス化・実データ検証完了（2026-07-06）。
  実装は00:47-00:56（`00222d5`/`5a30681`）、実データ検証は21時台に本セッションで実施
  （`docs/daily作業ログ/20260706_213550.md`）。検証中に`scripts/build_version_diff_poc.py`の
  `pptx_lines()`がPPTXのテーブルシェイプ内テキストを一切抽出していないバグを発見・修正（`1f994ce`）。
  さらに**本番`src/parsers/office_parser.py`にも同種のバグ**があり修正（`a818a76`、検索基盤全体に影響。
  strategy.md §3.2の「検索精度は全タイプの土台」に該当する副産物的な発見）。
  official judge較正: valid Q9（唯一の該当問）**Missing→Perfect**、official mean **0.3667→0.4667**
  （`judge_calibration_1783341078.json`）。no-harm確認済み（新規Incorrectなし、既知Q17のみ）。
  test 10問（valid以外）はオフラインのペア特定確認のみで生成・judge較正は未実施（残課題）
- [x] **contract_rule（実装＋レビュー修正、2026-07-06〜07）**: `build_contract_registry.py`/`contract_calc.py`/`approval_rule.py`実装（`afe0d2d`）。
  実装直後のレビューで、実際の`question_labels.csv`文言と照合すると主対象9問中5問（Q26/Q31/Q37/Q38/Q76）が
  ガード条件・正規表現・計算基準の不一致で機能していなかったことが判明（計画書の設問要約をそのまま
  ガードにしたため、実文言と一致しなかった）。TDD＋step-reviewで6件修正（`01349af`/`88fe1d1`/`8ce3715`）。
  test 100問診断run（`--no-judge`、`contract_fix_test_diag_1783351147.json`）で全経路の発火を確認:
  Q6=`contract_final_diff`(0円減額)、Q23=`contract_calc`(398,750円減額)、Q26=`contract_overlap`
  (TOTO、AOMINE)、Q31=`contract_per_row`(MINAMINO、1,320円)、Q37=`contract_derived_rate`(22,000円)、
  Q55=`contract_hours_gap`(AOMINE)、Q76=`contract_calc`(79,200円増額)。例外0件。
  Q38はAPR閾値バグ修正後、実データ内に真のAPR-M3該当がなく（かえで契約書が暗号化未復号のため）Missingへ
  安全にフォールバック（旧実装は白峰を誤ってAPR-M3と回答しておりIncorrectだった→修正後はMissingで安全側）。
  valid 30問run（`contract_fix_valid_1783350883.json`）で regression なし確認（mean 0.3667、旧vdiff2_valid
  と同値。差分3問はいずれも本ブロック無関係の generic LLM path variance）。
  残課題: Q6の`final_amount_incl_tax`が最終報告書の「実績工数未確定のため見込工数を精算想定値として記載」
  という明示的な仮定値をそのまま実績値として扱っている点（Missingにすべきか要検討）、かえで契約書の復号
  （`データアステル社内規定_パスワード導出規則.docx`のルール適用が未着手、下記「パスワード保護ファイル」参照）、
  Q46/Q67/Q78/Q79/Q86/Q87/Q98は未実装（計画通り優先度低・別ブロック）。
- [x] **cross_project（9問、2026-07-11）**: `docs/plan/2026-07-11-cross-project-block.md`のTask1〜3実装。
  Q87完了判定バグ修正（`has_final_report`導入、`4fd4788`）、Q3消費税総額ルーティング（`ff29b73`）を
  実データ検証したところ、①`artifacts/contracts.jsonl`がスキーマ変更後未再生成、②`pipeline.py`が
  cross_projectタグのMissing判定を「非該当」と誤解釈しretrieval+LLMへフォールスルーする隠れバグ
  （Q3のIncorrect解消が実際には機能していなかった）を発見・修正（`f14b859`）。valid診断run(N=3)で
  Q3が安定して`structured:cross_project`のMissing（`gate_reason: contract_tax_total_incomplete`）に
  なることを確認、regressionなし。Q40（支払月ごと精算総額top3）を`payment_schedule`抽出＋
  `_answer_payment_top3`で実装（`d334dc3`）、実データでの正解は**2025年10月: 11,412,500円、
  2025年9月: 9,350,000円、2025年8月: 8,415,000円**（ブロック計画書§1.3の暫定値はかえでの支払未算入で
  実装後に上書き）。Q67は`paid_amount_incl_tax()`流用で9候補中8件は解決可能と判明したが、
  青潮1件が最終報告書画像PDF（テキスト層なし）でQ3と同一理由で解決不能なため「全候補解決必須」の
  安全ゲート下で実装してもMissingから変わらず、期待値ゼロと判断し非実装。Q13/Q46（座席表OCR）・
  Q86（people_registry）は計画通りスコープ外（Phase 4待ち）。詳細は`docs/daily作業ログ/20260711_220906.md`
- [x] **パスワード保護ファイル（2件、2026-07-12）**: 契約書_pw-kaede20250902.docxは`build_contract_registry.py`側で既に復号配線済み（`contracts.jsonl`status="ok"）、Q38はAPR-M3該当0件でMissingが正解と確認済みのため追加対応不要。スケジュール.xlsx（`encrypted_or_legacy_office_container`）はDA規則`DA-KAEDE-20250902-xlsx`（日付候補源はcontracts.jsonlのstart_date、循環参照回避のためschedule_tasks.jsonlは使わない）で復号する処理を`scripts/extract_spreadsheets.py`へ配線（`6692190`、TDD 9テスト、step-review CONFIRMED 0件）。実データ再実行（`ea70afe`）でかえで総合病院のスケジュール.xlsxが4シート・538セル・37スケジュール行として抽出成功、`encrypted_file_queue.jsonl`から消去（`failures=0`）。このファイルに依存しそうなtest Q79（担当者別想定工数最大値）・Q92（ID発行数合計）は、該当する汎用集計answererが未実装のため今回はスコープ外とし、test 100問診断run（`pwfix_test_diag_1783858010.json`）でMissing継続・新規Incorrectなし・他94問の`answer_path`分布が直前のanalysisブロック診断と完全一致（回帰なし）を確認。詳細は`docs/daily作業ログ/20260712_210800.md`。残課題: Q79/Q92向けの新規集計answerer（担当者別工数集計、ID種別カウント）は未着手（test限定2問、valid該当なし）。

**完了条件**: valid の version_diff / contract_rule / cross_project 計5問で Incorrect 0 かつ Perfect 3以上。version_diffは達成（Q9 Perfect、Incorrect 0）。
**期待値**: 該当30問 × 成功率4〜5割 → **+0.12〜0.15**。

### Phase 4: 残タイプと精度の底上げ

- [x] **analysis_metrics / notebook_output / code_static（primary 12問、2026-07-12実装・較正済み）**: 最小artifact生成（`59a3fb1`）、JSON/AST短答（`8f0a94a`）、Notebook相関再計算（`670b061`）、複数成果物比較（`b2215e6`）、gap fix（`Pipeline._process_one`のanalysisタグ許可漏れ・One-Hot対象列test Q73・CAT閾値symbolic表示valid Q28、`70fd3c2`。step-review CONFIRMED 0件）を実装。**565 tests PASS**。正しいdata-dir=`data/raw/share/共有ドライブ`での直接診断は対象12問中11問が`structured:analysis`、test Q56のみ計画どおり安全なMissing。
  **valid N=3+official較正（2026-07-12、`phase4analysis_gapfix_valid_r1〜r3`、較正は`judge_calibration_1783848948.json`）**: official mean **0.6333**（r1）。target 5問（Q4/Q22/Q24/Q27/Q28）は3run・official較正いずれも**Incorrect 0、official Perfect 5/5**（local judgeはAcceptable/Missing判定のブレあり、officialでは全問Perfectに収束）。新規official Incorrectは0——較正でIncorrectだったのはQ17（既知・2026-07-04から未解決の言い換え問題、next-steps §4で対応保留）とQ18（既知・2026-07-04から続く章番号系の長期不安定問題、analysis変更とは無関係な既存事象）の2問のみで、いずれも本ブロックの実装以前から記録済み。
  test側（Q4=`bmi`、Q5=`6`、Q32=交互作用6列、Q35=`0.90527`、Q61=`300/0.1/42`、Q73=閾値100未満・`Gender`）はGT非公開のためLB確認待ち。**採否: 採用**（valid側completion条件達成）。
- [x] **image_graph型の一部（2026-07-11〜12）**: `docs/superpowers/plans/2026-07-11-phase4-image-graph-block.md`のTask1〜8実装（subagent-driven-developmentで8タスク・全task-review Approved）。
  実データ調査で判明: image_graph主・副タグが付く7問中3問（test Q39/Q33/Q54）は実はVLM不要——xlsx/docxの**ネイティブ埋め込みチャート（chartEx/chart XML）にキャッシュされた実数値**を直接抽出できる
  （strategy.md §3.4で想定していた「クエリ時画像参照」より前段の、より確実な機械抽出手段）。
  `src/generator/office_chart.py`に`extract_xlsx_chart_series`/`extract_office_chart_series`/`OfficeChartAnswerer`を新設し
  `pipeline.py`の`_process_structured`へ配線（`02c098f`〜`b28c21e`）。残る2問（valid Q1・test Q66、KSSの`figure_06.png`）は
  真にVLMが必要なためstrategy.md §3.4の「クエリ時画像参照」方式で`src/generator/vlm_answerer.py`に`VLMImageAnswerer`
  （Claude Vision API、Anthropicクライアントへ画像contentブロックを追加）を新設・配線（`2e5143f`〜`8447928`）。
  実装中に3件の実バグを発見・修正（いずれも「\w が平仮名を含みUnicodeの助詞境界を吸収する」「案件名の部分文字列が色語と衝突する」
  「疎な系列のNaN値がconfidence 0.9の確信ある誤答として返る」という、案件名・実データに依存する非自明なバグで、
  ユニットテスト作成時に発覚→即修正・回帰テスト追加）。
  実データ検証（test 100問診断run + valid 30問run/official較正`judge_calibration_1783838926.json`）:
  test Q39=`hum`・Q33=`0.19555`・Q54=`0.50239`（いずれも事前計算値と完全一致、`structured:office_chart`）、
  valid Q1=`structured:vlm_image`「20日」で**official Perfect確定**。新規official Incorrectなし（既知のQ17・Q25パターンのみ、
  本ブロック無関係）。test Q66は質問文にファイル名の明記がなく`find_referenced_image`が不発のため未解決のままだが、
  `IMAGE_KEYWORDS`に`"可視化"`を追加したことで`image_or_graph`タグが付くようになり、以前の`text_only`扱い（無防備な
  生成LLM推測、Incorrectリスクあり）から`confidence_gate.py`の`CAPABILITY_BLOCKED_TAGS`による安全なMissingへ転換した
  （守りの改善、正答化はせず）。詳細は`docs/daily作業ログ/`の該当ログを参照。
  残る座席表OCR（test Q13/Q46）・青潮06.報告書の画像のみPDF（valid Q3・test Q67の部分ブロッカー）・notebook出力画像
  （valid Q24・test Q56、pandas再計算の方が筋が良い可能性）は計画通りスコープ外（別ブロック候補として計画書に記載済み）
- [x] Phase 1〜3 の flip 分析で見つかった残存 Incorrect パターンの個別（ただし汎用的な）対策（2026-07-12〜13、`f59c732`）: Q18の恒久対策として`docs/daily作業ログ/20260711_014012.md`のN=5診断（検索は決定論的に正解チャンクを取得済みだがdistractor文書混入がLLMの章番号取り違えを誘発）を追跡。原因は`QueryExpander`の略号展開（"CT"→"契約書"等、`artifacts/term_registry.json`）が`ProjectScopedRetriever`の名指しファイル優先ロジックに未接続だったこと。CT/PL/PP/MM/APR/EXT/TXの計7種の文書種別略号すべてに共通する一般化可能な穴（Q18固有ではない）と判明。`find_stem_matches()`を新設し、QueryExpanderが実際に適用した展開語（`applied_expansions()`）が候補文書のbasename stemと一致する場合に名指しファイル優先を発火させるよう配線（ソフトな並べ替え、ハードフィルタにはしない）。TDD実装、`.venv/bin/pytest tests/ -v` 597 passed（回帰なし）。実データ検証: valid Q18のretrieved_sourcesが契約書.docx 1/5→4/5に改善し誤答"4"→正解"3"に変化、test 100問診断run（`docscope_test_diag_1783862198.json`）でCT/PL/PP/MM/TX関連7問のretrieved_sources変化を確認、answer_path分布は完全一致（新規ルーティング破壊なし）、新規Incorrectリスクなし。Q17（言い換えIncorrect）は別原因（LLM生成側のパラフレーズ）で今回は対象外——`temperature=0`によるブレ抑制はclaude-sonnet-5がtemperature指定でAPI 400のため不可と再確認済み（`docs/daily作業ログ/20260704_003905.md`）。詳細は`docs/daily作業ログ/20260712_222808.md`。
  **valid N=3+official較正（2026-07-13、`docscope_valid_r1/r2/r3`・`judge_calibration_1783945810.json`）**: local mean 0.5167/0.5667/0.5333、Incorrectは3run共通でQ25（既知の書式抽出パターン、本変更と無関係）のみ。official較正（r1）でofficial mean **0.6000**、official Incorrectは2問（Q17=既知の言い換え問題、Q28=前回較正時official Perfectだった同一回答文がjudge非決定性でIncorrect化——answer文字列が前回runと完全一致していることを確認済み）。**新規official Incorrect 0件、採用確定**。test predictions.csvを`make_predictions.py --runs 3`で再生成（回答34→32件）、diffはdocscope対象問の改善（Q75「Missing→4週目」等）と既知のLLM生成ブレ（Q8/Q9/Q94のanswered⇄Missing反転）の混在で新規の危険な誤答パターンなし。`submission_20260713_docscope3run.zip`作成（`009800c`）、SIGNATE提出はユーザー判断待ち。詳細は`docs/daily作業ログ/20260713_213900.md`。
- [x] **ハイブリッド検索（BM25+日本語埋め込みベクトル、2026-07-13〜14）**: Q17を含む5問（Q0/Q10/Q14/Q17/Q19）が生成LLM側のパラフレーズ問題ではなく**BM25検索recall自体の欠陥**（カラム説明.mdが全7139候補中74位に埋没）と判明したため、BM25単体からBM25+ベクトルのハイブリッド検索（Reciprocal Rank Fusion、`docs/superpowers/plans/2026-07-13-hybrid-retrieval.md`、全8タスクsubagent-driven-developmentで実装）へ移行。
  `Embedder`抽象化（`HashEmbedder`/`JapaneseEmbedder`=cl-nagoya/ruri-base/`CachedEmbedder`=SHA256重複排除）、`VectorStore`のEmbedder委譲、`HybridRetriever`のRRF統合（`vector_weight`/`keyword_weight`の重み付き合算を廃止）、`ProjectScopedRetriever(embedder=...)`のopt-in配線、`Pipeline(use_hybrid_search=...)`・3スクリプトの`--hybrid-search`フラグ配線まで完了（Task1〜7、`1ec8cc7`〜`83e95ca`、616 tests pass）。
  **実データ検証（Task8）で本番導入前にMPSセグフォルトを発見**: `Pipeline.run()`は`max_concurrent`本のスレッドから並行して`embed_query()`を呼ぶが、PyTorchのMPS(Metal)バックエンドはスレッドセーフでなく、複数スレッドが同時に`model.encode()`（および遅延ロード中の`SentenceTransformer()`構築）を呼ぶとセグフォルト（exit 139）する。`JapaneseEmbedder`にモデルロード・encode呼び出し全体を覆う`threading.Lock`を追加して修正（`f7ca4e7`）、実モデルへの20並列クエリ再現テストで修正前後の挙動を確認。
  検証結果: 検索recall 0.655→**0.793**（valid29問中、eval_retrieval.py）、official judge mean 0.6000→**0.7333**（agreement 70%で不変）、indexing時間はembeddingキャッシュ温状態で102,263チャンク**約65秒**（3時間制限に十分収まる）。新規official IncorrectはQ6のみ（値は正しいが「平均/ALP」の集計内容記述が回答から欠落——検索成功によりMissing(0点)からIncorrect(-1点)に変化した副作用で、ハルシネーションではなく回答フォーマットの不足。Q17は既知パターンのまま継続）。
  ユーザー承認を得てデフォルトをON化（`d4a96fa`）: `Pipeline.__init__`自体のデフォルトはFalseのまま維持（既存テストの多くが素の`Pipeline()`にreal docsを流すため、Trueにすると実モデルダウンロードが走りテスト分離が壊れる）、代わりに3本のCLIスクリプト（`run_pipeline.py`/`make_predictions.py`/`eval_retrieval.py`）の`--hybrid-search`フラグのみdefault Trueに変更し`--no-hybrid-search`で明示オプトアウト可能にした。

### Phase 5: 較正・安定化・凍結（締切前 約2週間: 8/6〜8/20）

- [x] `confidence_threshold`（タイプ別）のオフライン探索基盤（2026-07-16, `00990f1`）: `routing_tags`のみを使い、開発/holdout分離、最低件数、引き上げのみの反実仮想評価、最高/保守構成を実装。`q6_noextra_valid` N=3の予備分析は両構成ともholdout 0.6333で現行と同率。
- [x] 3時間制限に対する本番実測（2026-07-16）: warm単発3分9.47秒、warm標準N=3 7分25.50秒で十分適合。一方coldはparse 14秒後、102,263チャンクの埋め込みindexだけで3時間32分49秒かかり、generation前に上限超過。**fresh-data cold構成は不適合**。次は埋め込み初回構築の高速化または再利用可能な事前キャッシュ設計を優先する。詳細は`benchmark_warm_test_1784198120.json`、`predictions_stability_1784198585.json`、作業ログ`20260716_194413.md`。
- [x] cold不適合の解消（2026-07-16〜17）: ①同一バッチ内重複除去（`1505a3d`、102,386→ユニーク78,075件に削減）。②encode開始前の時間予算判定を実装（`589cec3`）——未処理ユニーク件数×プローブ実測throughput（ウォームアップ後1,024件等間隔サンプル）で見積もり、7,200秒超過またはthroughput実測不能ならBM25単体へ自動退避（`src/indexer/embedding_budget.py`、設計は`2026-07-16-cold-fallback-design.md`）。退避時はプローブ済みembeddingをflushしてから破棄（step-review CONFIRMED対応）。③実測: フルcold N=3実行は開始後に外部要因でkillされ完走せず（メモリ圧の兆候あり）、本番フォールバックと同一方式のプローブ実測で外挿——throughput **13.65チャンク/秒**、cold index **5,721秒（1.59h）**、cold N=3 E2E **約6,180秒（1.72h）**。3時間制限・2時間目標とも見込みクリア、予算7,200秒に対し余裕1.26倍（環境が26%以上遅い場合のみBM25退避が発火する安全網つき）。フルE2Eでの確認再実行はマシンに余裕がある時間帯に任意で行う。詳細は作業ログ`20260717_211207.md`。
- [x] 再現性検証（2026-07-17）: test単発×2は最終回答一致91%・answered/Missing 99%・answer path/検索結果100%一致（揺れ9問は全てgeneration段階、検索・ルーティングは決定論的）。N=3多数決の独立2バッチ比較は最終回答95%・answered/Missing 96%、不一致5問（Q8/Q9/Q41/Q69/Q97）は保守的構成のゲート候補。副産物として`normalize_answer`のU+2212/ハイフン非統一によるQ8の偽no_majority→Missingを発見（次実験候補）。詳細は`repro_audit_test_singles.json`・`repro_audit_test_n3.json`・作業ログ`20260717_213419.md`。
- [ ] **新機能フリーズ**（目安 8/13以降）: エッジケース処理・検収を意識したコード整理・README/実行手順の整備
- [ ] 提出戦略: 最終提出2枠は「最高スコア構成」と「保守的構成（ゲート強め）」で分ける。保守構成のルールは確定済み（2026-07-17、`b2eed00`）: `make_predictions.py --conservative`で、多数決勝者と内容の矛盾する少数派回答（部分文字列でも順序入れ替えでもない）がある質問をMissingへ倒す。反実仮想でvalidコストはQ9のAcceptable 1問（-0.017）、testはQ69型の真性不安定問のみゲート。一律の矛盾ゲート・全会一致要求はvalid -0.083で逆効果と実測済み（作業ログ`20260717_215015.md`）。最終提出物の生成はフリーズ時に実施。

---

## 4. 進め方の運用ルール

1. **各フェーズ着手時に `writing-plans` で実装計画を作る**（本書はロードマップであり実装手順書ではない）。実装は `dev-process` スキルのTDDルールに従う
2. **1実験1変更**。`experiments/run_<timestamp>.json` に保存し、flip分析（Phase 0で導入）を毎回確認。「仮説→期待ゲイン→実測→採否」を記録
3. **毎実験で Incorrect ゼロ維持を最優先で確認**する。新パス追加でIncorrectが増えたら、そのパスのゲートを強化してから次へ
4. **提出は各フェーズ完了ごとに1回**（LBとローカルの乖離監視）。LBの試行回数制限を確認の上、無駄撃ちしない
5. **汎用性チェック**: すべての新ロジックに「未知の案件・ファイル・質問でも同じ方針で動くか」を自問（規約: 特定案件名・ファイル名・質問への分岐は禁止。registryは実行時にデータから構築されるので適法、の整理を維持）

## 5. スケジュール目安（締切 8/20、チーム変更期限 8/5）

| 期間 | フェーズ | 出口目標（LB目安） |
|---|---|---|
| 7/3〜7/6 | Phase 0 | 計測基盤完成・失敗切り分け表 |
| 7/6〜7/13 | Phase 1 | +0.1〜0.2 |
| 7/13〜7/27 | Phase 2（＋Phase 3並行開始） | +0.35前後 |
| 7/27〜8/6 | Phase 3・4 | +0.45〜0.5 |
| 8/6〜8/20 | Phase 5（8/13頃フリーズ） | 較正で上積み・安定化 |

※ LB目安は「該当問数×成功率見込み」の粗い見積もり。Phase 0 の切り分け結果と各フェーズの実測で毎回更新する。validは30問しかなく1問=3.3%揺れるため、タイプ別内訳で判断する。

## 6. 直近アクション(2026-07-15時点)

**2026-07-16追記**: Q17の質問文にある拡張子なしの実在stem「カラム説明」をファイルスコープとして扱う汎用修正を実装（`45a2048`）。valid検索recallは0.793→**0.862（25/29）**、Q17は`カラム説明.md`がrank 1、single_text 4/4。629 tests pass。valid N=3はlocal mean 0.6167/0.6333/0.6167、Q17は3runとも「未連絡」。r1 official meanは直前版0.7333→**0.8000**、Q17のみIncorrect→Perfect（+2/30）。新規official Incorrect 0のため採用確定。

**2026-07-16 Q6対策（`6ca3746`）**: Pivotの条件・集計内容を返す機械直答が、最大行の完全性確認に使った集計値まで回答へ付加していた。値の存在確認は維持し、最終回答を質問要求どおり「抽出条件＋集計内容」に限定。TDDで修正前の失敗を確認後、対象2 tests・全631 tests pass。外部通信なしの実データQ6 N=3は3回とも「抽出条件: Gender = Male、disease = 1、Age = 68、集計内容: 平均 / ALP」、`structured:spreadsheet_state`、ungatedで完全一致。step-review問題なし。full valid N=3とofficial較正は外部APIへの非公開データ送信に明示承認が必要なため未実施。

**2026-07-16 Q6 full較正**: ユーザー承認後、`q6_noextra_valid_r1〜r3`を実行。local mean 0.6500 / 0.6167 / 0.6333、Q6は3run同一回答・local Perfect、answer pathとretrieved sourcesも修正前から不変。r1 official較正（`judge_calibration_1784184775.json`）はmean **0.8333**（修正前0.8000）、Q6のみIncorrect→Missing、他29問のofficialラベル変更0、official Incorrect 0。最低目標+1/30を達成し、新規退行がないため採用確定。

**2026-07-16提出準備**: test 100問のN=3生成を完了し、`submission_20260716_q17-filescope3run.zip`を作成。初回実行で`make_predictions.py`のみ埋め込み永続キャッシュが未配線のため2時間でも索引構築未完了となる問題を発見。`2615251`で`cache_dir=ROOT/.cache`と`--no-cache`を配線し、索引構築を約66秒に回復。631 tests pass。CSV形式・100ID・zip内容検証済み、SIGNATE手動提出待ち。

**2026-07-16 local judge較正の改善**: `judge_calibration_*.json`を質問×回答×GTで重複除去したdev/holdout回帰データ（`cb88bb4`）と解析エラー1回再試行（`17ce9fc`）を実装後、dev stable agreementが81.6%で85%未達だった。誤判定7件中3件がQ17パターン（GT「未連絡」への同義言い換えをAcceptable誤判定）で、プロンプトのルール4（短答言い換えはIncorrect）とルール5（軽微な不完全さはAcceptable）の衝突が原因と特定。ルール6でルール4を優先するよう修正（`3413b52`、TDD・step-review済み、669 tests pass）。dev再判定でstable agreement**89.5%**（2回完全再現、揺らぎでないことを確認）。holdoutで初較正しstable agreement**89.7%**（39件中35件一致、Incorrect recall 0.333、MAE 0.103）、修正前baselineより全指標改善・悪化なしのため採用確定。Q17パターンは4件中1件のみ修正され3件は依然Acceptableのまま（モデルがルール4該当を認識しつつ軽微と判断し上書き）、残課題として継続監視。

**2026-07-18〜19 test Missing診断と第1波修正**: test all_missing 57問を4サブエージェント並列で診断（レポート`docs/test_missing_*_diag_20260718.md`）。57問中53問はconfidenceゲートでなくLLM自身の「わかりません」であり、閾値でなく能力・検索の問題と確定。画像起因Missingが計12問超（当初見積もり2問を大幅超過）でOCR/VLM再評価を別枠化。期待値上位3件を実装・採用: ①スケジュール行マッチング汎用化`e88aae9`（診断6問中3問が正解行到達） ②docxテーブル抽出`29eedaa`（社内用語集134→7,681文字、docx46件中35件の表が索引可能に） ③office_style AND絞り込み＋分類キーワード＋過剰列挙ガード`af65451`（Q16: 装飾375件→正解候補2件）。valid検索recall 25/29でベースライン0.862から退行なし。valid N=3（`wave1_valid_r1〜r3`）はlocal mean 0.8333/0.8167/0.8333・**Incorrect全run 0**、回答経路の変化ゼロ（判定値の上振れはjudgeプロンプト改善`3413b52`による同一回答への再判定。r3は全30問byte一致）。Q15採点キー順序不一致も解消（実害なし確認済み、`docs/q15_valid_scoring_key_check_20260718.md`）。詳細は`docs/plan/2026-07-18-next-steps.md`・作業ログ`20260719_015735.md`/`20260719_020500.md`。

**2026-07-19 第2波修正4件**: fingerprint恒久策`ea4098f`・xlsxシートスコープ`7badf9a`（Q80/82/47）・暗号化検知内容ベース化`a388400`（Q79、KAEDEスケジュール5,803文字索引化）・回帰予測answerer`a6ce5cb`（Q63/83、Decimal ROUND_HALF_UP）。採用ゲート: recall 25/29不変、wave2 valid N=3 local 0.85/0.85/0.83・Incorrect全run 0・gate/path変化ゼロ。test側の期待効果はQ79/Q63/Q83の新規回答化とQ80/82の誤答混入防止。詳細は作業ログ`20260719_175544.md`。

現状: Phase 4ブロック群＋docscope（`f59c732`）＋ハイブリッド検索（BM25+日本語埋め込みベクトル、RRF統合）がHEADに反映済み（`047ccd1`まで）。CLIデフォルトはハイブリッド検索ON。valid実データ検証でrecall 0.655→0.793・official mean 0.6000→0.7333の改善を確認済み。`submission_20260715_hybridsearch3run.zip`提出、**LB 0.2（=6/30、前回docscope3runと同値・net 0）**——valid/official側の明確な改善に対しLBは横ばいだが、公開LBのサブセット粒度に起因する既知の乖離パターンと判断し採用継続（§1.1.1参照）。

1. Q17（言い換えによるIncorrect）はハイブリッド検索でも未解決（ファイルレベルでは依然カラム説明.mdが上位に来ない）。次の一手はretrieval側の追加チューニングよりも、Phase 5の較正・安定化作業と並行して収穫逓減を見極める方が優先度が高い可能性がある。
2. Q6のように「検索成功→回答フォーマット不足でIncorrect化」する副作用パターンが今後増える場合は、生成プロンプト側で「集計内容も明記」等のフォーマット指示を検討（Q6固有の対策ではなく、同種の質問パターン全体への一般化可能性を確認してから着手）。
3. Q79/Q92向けの新規集計answerer（test限定2問）は着手判断を保留中（次アクション候補としてnext-steps.md §3参照）。
4. LB横ばいが続く場合、公開LBへの追加投資（コード変更の判断材料としての信頼度）を下げ、Phase 5（confidence threshold調整・時間実測・再現性検証）への切り替えを検討する。
