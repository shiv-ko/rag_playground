pipeline:
  top_k_retrieval: 5
  max_answer_tokens: 1000
  confidence_threshold: 0.4   # これ以下はMissing扱い
  max_concurrent_requests: 5
  request_timeout_seconds: 60

retriever:
  vector_weight: 0.6
  keyword_weight: 0.4
  rerank_top_n: 3

generator:
  temperature: 0.0
  max_tokens: 1000

judge:
  temperature: 0.0

logging:
  level: INFO
  experiments_dir: experiments/
